# Red drill 2026-09-09

The two maintained bootstrap drills both failed under their intended mutations.

| Drill                                             | File                                  | Command                                                                     | Result   |
| ------------------------------------------------- | ------------------------------------- | --------------------------------------------------------------------------- | -------- |
| Bootstrap: nullable account generation is ignored | `src/client/lib/bootstrap-request.ts` | `vitest run --project unit-client src/client/lib/bootstrap-request.test.ts` | red (4s) |
| Bootstrap: mismatched membership role is accepted | `src/shared/bootstrap.ts`             | `vitest run --project unit-client src/shared/bootstrap.test.ts`             | red (4s) |

## Browser diagnostic privacy

New client and real-Worker tests first reproduced raw private message, URL and
user-agent retention before the fix: two intended assertion failures. After
classification/source/route normalization and removal of raw user-agent storage,
all 28 focused tests passed.

A separate green/red/restored-green drill then replaced only
`message: classifyClientError(errorClass)` with `message: errorClass` in the
browser reporter. The real outgoing Blob assertion failed because it contained
the private-content canary. The source was restored byte-for-byte to SHA-256
`9ae1139fe42c25248ff8f257e1e93d948b0925aaa93462794d61af20b8daad96`, then all 28
client/shared/Worker diagnostic tests passed again. The source restoration check
ran before restored verification; no mutant remains in product code.

Ignored logs: `temp/lumafoil-telemetry-privacy-{red,green}.log` and
`temp/lumafoil-telemetry-{drill-red,drill-restored}.log`. This focused proof does
not replace aggregate quality, current global coverage or hosted logging checks.
