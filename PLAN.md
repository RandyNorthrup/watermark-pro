# PLAN.md — Watermark Pro

Living planning document. Decisions, assumptions, open questions, architecture,
milestones, and certification gates. Update it whenever a decision changes.
`CHANGELOG.md` records what happened; this file records what is intended and why.

Last updated: 2026-09-06 (M2 certified)

---

## 1. Product summary

Watermark Pro is a web application for watermarking photographs, positioned as a
spiritual competitor to eZy Watermark. It is MIT licensed, hosted on Cloudflare
Workers at `watermark.blowmoney.net`, and is built to enterprise-grade security
standards with role-based access control.

### Required capabilities (from the product brief)

| #   | Capability                                                                                                                               | Milestone                            |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| R1  | Watermark photos with text, symbol, or image watermarks                                                                                  | M2, M4                               |
| R2  | Bulk watermarking of many photos in one job                                                                                              | M5                                   |
| R3  | Library of saved, reusable watermarks                                                                                                    | M3                                   |
| R4  | Smart Apply: analyse each photo and choose the best watermark placement                                                                  | M2                                   |
| R5  | Watermark contrast auto-chosen by default, manually adjustable                                                                           | M2, M4                               |
| R6  | Watermarked photos stored in the app after processing                                                                                    | M6                                   |
| R7  | Share from the app                                                                                                                       | M7                                   |
| R8  | Export in multiple image formats                                                                                                         | M5                                   |
| R9  | Editor to create custom watermarks and apply library watermarks                                                                          | M4                                   |
| R10 | Editor supports crop and resize                                                                                                          | M4                                   |
| R11 | Wide variety of fonts and symbols to choose from                                                                                         | M3                                   |
| R12 | Fully secure, enterprise grade                                                                                                           | every milestone; hardening in M8     |
| R13 | RBAC for users                                                                                                                           | M1, M8                               |
| R14 | Modern, beautiful UI and UX                                                                                                              | M1 design system; every UI milestone |
| R15 | Hosted on the owner's Cloudflare account at **watermark.blowmoney.net** (updated 2026-09-05; the brief originally said no custom domain) | M0 config, M8 certification          |

---

## 2. Assumptions

Each assumption is a default taken because the choice is cheap to reverse now
and expensive to block on. If any is wrong, say so and the plan will be revised.

| ID  | Assumption                                                                                                                                                                                                                                                                  | Why this default                                                                                                                                                                          |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | Package manager is **npm 11** (lockfile v3).                                                                                                                                                                                                                                | Installed, no pnpm/yarn present.                                                                                                                                                          |
| A2  | CI provider is **GitHub Actions**; the repository is **public** on GitHub.                                                                                                                                                                                                  | Confirmed by the owner on 2026-09-05. Public repo means gitleaks, pinned actions, and `SECURITY.md` matter from the first commit.                                                         |
| A3  | Frontend is a **single-page application** (no SSR).                                                                                                                                                                                                                         | Authenticated tool, not a content site; SPA keeps the Worker stateless and the CSP strict.                                                                                                |
| A4  | Image rendering runs **in the browser** (Web Worker + OffscreenCanvas), not on Cloudflare Workers.                                                                                                                                                                          | Workers CPU limits make bulk raster work impractical and costly; browser rendering is free, private, and parallel. Server stores results only.                                            |
| A5  | Authentication is **email + password with sessions**, self-hosted via Better Auth on D1, with the organization plugin for RBAC. Passkeys may be added later as a second factor.                                                                                             | No identity provider was specified and SSO was declined (Q2). The Paid plan CPU budget (A13) makes a proper password hash affordable; M1 still records the measured cost.                 |
| A6  | Transactional email goes through **Cloudflare Email Sending** (the `send_email` binding) from `no-reply@watermark.blowmoney.net`, behind a provider interface. A `console` provider exists for local development and tests; the app refuses to start with it in production. | Sender subdomain chosen by the owner on 2026-09-06 (Q9) so the apex domain's mail records stay untouched.                                                                                 |
| A7  | Fonts are **self-hosted** open-licensed families (OFL/Apache) bundled from `@fontsource` packages.                                                                                                                                                                          | Keeps CSP strict (`font-src 'self'`) and avoids third-party requests from an enterprise app.                                                                                              |
| A8  | Browser support: **evergreen browsers with OffscreenCanvas** (Chrome/Edge 69+, Firefox 105+, Safari 16.4+).                                                                                                                                                                 | Required for off-main-thread rendering.                                                                                                                                                   |
| A9  | Photo formats accepted for upload: JPEG, PNG, WebP, AVIF, GIF (first frame), HEIC where the browser can decode it.                                                                                                                                                          | Browser `createImageBitmap` coverage.                                                                                                                                                     |
| A10 | Export formats: **PNG, JPEG, WebP** in M5 (native `canvas.convertToBlob`), **AVIF** via WASM encoder as an M5 stretch item.                                                                                                                                                 | Native encoders are zero-dependency; AVIF needs `@jsquash/avif`.                                                                                                                          |
| A11 | The single origin is `https://watermark.blowmoney.net`; the `workers.dev` subdomain is disabled (`workers_dev: false`).                                                                                                                                                     | Owner supplied the domain on 2026-09-05. One origin keeps cookies, CSP, and share links unambiguous.                                                                                      |
| A12 | The owner has approved installing any tooling the project needs on this machine ("you install whatever is needed", 2026-09-05).                                                                                                                                             | Global installs are still avoided where a local devDependency works; machine-level installs are listed in README → Requirements.                                                          |
| A13 | Cloudflare **Workers Paid** plan ($5/month) for production, upgraded from Free on 2026-09-05.                                                                                                                                                                               | Stated by the owner. Gives 30 s CPU per request by default (raisable to 5 min with `limits.cpu_ms`), D1 and R2 paid quotas, and Email Sending. Rendering still stays in the browser (A4). |

---

## 3. Resolved decisions

### 3.1 Stack

| Layer           | Choice                                                                                            | Pinned version                                  | Source of verification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Language        | TypeScript                                                                                        | **6.0.3**                                       | `npm info typescript-eslint peerDependencies` → `typescript: '>=4.8.4 <6.1.0'` (checked 2026-09-05). TypeScript 7.0.2 is latest but would silently disable every type-aware lint rule.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Runtime         | Cloudflare Workers, single Worker serving Static Assets + API                                     | wrangler **4.129.0**                            | `@cloudflare/vite-plugin` peer `wrangler: '^4.129.0'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Build           | Vite                                                                                              | **8.2.2**                                       | `@vitejs/plugin-react@6.1.1` peer `vite: '^8.0.0'`; `@cloudflare/vite-plugin@1.54.4` peer `vite: '^6.1.0 \|\| ^7.0.0 \|\| ^8.0.0'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| UI              | React                                                                                             | **19.2.8**                                      | `@testing-library/react` peer `^18 \|\| ^19`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Routing         | TanStack Router (file-based, type-safe)                                                           | **1.170.32**                                    | `@tanstack/router-plugin` peer `@tanstack/react-router: '^1.170.32'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Data fetching   | TanStack Query                                                                                    | **5.102.8**                                     | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Styling         | Tailwind CSS 4 via `@tailwindcss/vite`                                                            | **4.3.3**                                       | peer `vite: '^5.2.0 \|\| ^6 \|\| ^7 \|\| ^8'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Primitives      | Radix UI (`radix-ui` unified package), lucide-react icons                                         | 1.6.7 / 1.41.0                                  | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| API             | Hono                                                                                              | **4.13.7**                                      | exports `./secure-headers`, `./csrf`, `./cors`, `./validator` confirmed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Validation      | Zod 4                                                                                             | **4.5.4**                                       | better-auth depends on `zod ^4.3.6`; single Zod major across the project.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Database        | Cloudflare D1 via Drizzle ORM                                                                     | drizzle-orm **0.45.2**, drizzle-kit **0.31.10** | better-auth peer `drizzle-orm: '^0.45.2 \|\| >=1.0.0-rc.1 <2.0.0'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Object storage  | Cloudflare R2                                                                                     | binding                                         | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Rate limiting   | Workers Rate Limiting binding                                                                     | binding                                         | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Auth + RBAC     | Better Auth (organization + admin plugins)                                                        | **1.7.2**                                       | peer ranges above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ZIP export      | fflate                                                                                            | **0.8.3**                                       | MIT, streaming, small.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Unit tests      | Vitest                                                                                            | **4.1.11**                                      | `@cloudflare/vitest-pool-workers@0.22.0` peer `vitest: '^4.1.0'`. Vitest 5.0.0 is latest but unsupported by the Workers pool.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Worker tests    | `@cloudflare/vitest-pool-workers` (real D1/R2 in Miniflare)                                       | **0.22.0**                                      | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| E2E             | Playwright against the production build in workerd, `@axe-core/playwright` on every page          | 1.63.0 / 4.13.0                                 | `npm run test:e2e`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| UI kit          | Radix UI unified package, lucide-react, class-variance-authority, clsx, tailwind-merge            | 1.6.7 / 1.37.0 / 0.7.1 / 2.1.1 / 3.6.0          | lucide-react 1.41.0 was refused by `min-release-age=7`; 1.37.0 is the newest release older than seven days on 2026-09-06.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Fonts           | `@fontsource-variable/inter` (self-hosted)                                                        | 5.3.0                                           | OFL licence; no third-party font origin, so `font-src 'self'` holds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Test tooling    | `@testing-library/user-event`, `@better-auth/core` (drift test only), lighthouse, chrome-launcher | 14.6.1 / 1.7.2 / 13.4.1 / 1.2.0                 | `@better-auth/core` is pinned to the better-auth version it ships with.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ZIP             | fflate                                                                                            | **0.8.3**                                       | MIT, zero dependencies, streaming `Zip` with stored entries; also used in tests to unpack downloads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| QR codes        | qrcode-generator                                                                                  | **2.0.4**                                       | MIT, zero dependencies, ships its own types, exposes the module matrix (`isDark`) so the engine draws the pixels itself; the M10 plan asked for a dependency-free encoder, which this is.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| EXIF read (M13) | exifr                                                                                             | **7.1.3**                                       | MIT, zero dependencies, browser build. Reads the camera fields for the metadata tokens; the raw Exif/XMP/density bytes come from our own scanner. Its `.d.ts` types `ifd0` as non-boolean and omits `chunked`, and translates Orientation to a string under `translateValues`, so options pass `translateValues: false` and drop those keys; called via its named `parse` export.                                                                                                                                                                                                                               |
| OneDrive (M16)  | `@azure/msal-browser` (bundled, not CDN)                                                          | **5.20.0**                                      | MIT, no peer dependencies (`engines.node >=0.8`). Newest release before the registry cutoff (5.21.0 was too new); pinned exact. Only OneDrive import uses it, for the MSAL popup sign-in that yields a Microsoft Graph `Files.Read` token; Google and Dropbox load their SDKs from a CDN instead.                                                                                                                                                                                                                                                                                                               |
| Video (M17)     | `mediabunny`                                                                                      | **1.55.5**                                      | MPL-2.0, no peer dependencies (declares its own WebCodecs types). Newest release **≤ the 2026-08-31 registry cutoff** (1.55.7 is post-cutoff); pinned exact. Demuxes, decodes and re-muxes MP4/WebM/MOV around WebCodecs for the Video tool; supersedes `mp4-muxer`/`webm-muxer`. Loaded only on `/app/video` via the `video` chunk group.                                                                                                                                                                                                                                                                      |
| PDF (M17)       | `pdf-lib`                                                                                         | **1.17.1**                                      | MIT, no peer dependencies. Browser-side PDF load/embed/draw/save for the Documents tool (`src/client/pdf/watermark-pdf.ts`). **Unmaintained** (last release 2021); the maintained fork `@cantoo/pdf-lib` is the drop-in migration target if a fix is ever needed — none is required for the load/embed/drawImage/save path in use. Pinned exact; loaded only on `/app/documents` via the `pdf` chunk group.                                                                                                                                                                                                     |
| i18n (M18)      | `i18next`, `react-i18next`                                                                        | **26.4.0 / 17.0.12**                            | MIT. Newest releases at least seven days old and ≤ the 2026-08-31 registry cutoff (26.4.2 / 17.0.13 were younger than `min-release-age=7`); pinned exact. Peers satisfied: `react-i18next` wants `i18next >= 26.2.0` (26.4.0 ✓), `react >= 16.8` (19 ✓), `typescript ^5 \|\| ^6 \|\| ^7` (6 ✓). No `i18next-http-backend` (catalogues bundled + code-split), no browser language-detector (detection is `i18n/detect.ts`), no ICU plugin (JSON v4 plurals use `Intl.PluralRules`). `eslint-plugin-i18next` **6.1.5** (ISC, dev) enforces the `no-literal-string` gate on `src/client/**/*.tsx` (jsx-text-only). |

### 3.2 Quality gates

| Gate         | Tool                                                                                                                                                                                            | Version                                                                                                           | Command                                 |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| Format       | Prettier (+ tailwind class sorting)                                                                                                                                                             | 3.9.6 / 0.8.1                                                                                                     | `npm run format:check`                  |
| Lint         | ESLint flat config, `strictTypeChecked` + `stylisticTypeChecked`, unicorn, import-x, react-hooks, react-refresh                                                                                 | eslint 10.10.0, typescript-eslint 8.69.0, unicorn 74.0.0, import-x 4.17.1, react-hooks 7.1.1, react-refresh 0.5.6 | `npm run lint` (`--max-warnings=0`)     |
| Types        | `tsc --noEmit` per project reference, strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` + `noImplicitOverride` + `noPropertyAccessFromIndexSignature` + `verbatimModuleSyntax` | 6.0.3                                                                                                             | `npm run typecheck`                     |
| CSS          | stylelint + `stylelint-config-standard` (Tailwind at-rules allowlisted)                                                                                                                         | 17.15.0 / 40.0.0                                                                                                  | `npm run lint:css` (`--max-warnings=0`) |
| Dead code    | knip (`--treat-config-hints-as-errors`; **never** `--strict`, see §3.3)                                                                                                                         | 6.34.0                                                                                                            | `npm run deadcode`                      |
| Cycles       | dpdm (`--exit-code circular:1`)                                                                                                                                                                 | 4.3.0                                                                                                             | `npm run lint:cycles`                   |
| Duplication  | jscpd                                                                                                                                                                                           | 5.1.2                                                                                                             | `npm run lint:dup`                      |
| Secrets      | gitleaks (pre-commit `--staged`, `security:secrets` over history, CI over full history)                                                                                                         | 8.30.1 (machine and CI)                                                                                           | `npm run security:secrets`              |
| Dependencies | `npm audit --audit-level=high`                                                                                                                                                                  | npm 11.19.0                                                                                                       | `npm run security:audit`                |
| SAST         | semgrep, rulesets `p/default`, `p/typescript`, `p/react`, `p/secrets` (`--config auto` requires telemetry and is not used)                                                                      | 1.174.0 (machine, `pip install semgrep`); CI container pinned by digest                                           | `npm run security:sast`                 |
| Tests        | Vitest with V8 coverage thresholds                                                                                                                                                              | 4.1.11                                                                                                            | `npm run test`                          |
| Build        | `vite build`                                                                                                                                                                                    | 8.2.2                                                                                                             | `npm run build`                         |
| Lighthouse   | `scripts/lighthouse.mjs <milestone> [desktop\|mobile]`: desktop preset and Lighthouse's default phone emulation, budgets from §5.5, signs in through the API for authenticated pages            |
| Screenshots  | `scripts/screenshots.mjs <milestone> [desktop\|phone\|tablet\|all]`: every screen, light and dark, at 1440×900 in Chromium and on an iPhone 14 and iPad Mini in WebKit                          |
| Devices      | Playwright projects `desktop-chrome`, `iphone` (WebKit), `ipad` (WebKit), `android` (Chromium): every journey on every device, axe on every page                                                |
| Red drill    | `scripts/red-drill.mjs`: applies each mutation in `scripts/red-drills.mjs`, runs the test or gate that must go red, restores the file; survivors fail the run (§8 M9)                           |
| Everything   | `npm run quality` (all gates except `security:sast` and `test:e2e`, which need machine tools; CI runs all three)                                                                                | —                                                                                                                 | fails on any blocking issue             |

### 3.3 Deferred gates

| Gate                        | Reason                                                                                            | Compensating control                                                                                                               | Revisit when                                  |
| --------------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| `eslint-plugin-jsx-a11y`    | Peer `eslint: '^3 … ^9'`; incompatible with ESLint 10.                                            | `@axe-core/playwright` in every UI e2e spec (M1+); Lighthouse Accessibility ≥ 95 at visual milestones.                             | jsx-a11y publishes ESLint 10 support.         |
| `eslint-plugin-react`       | Peer `eslint: '… ^9.7'`; incompatible with ESLint 10.                                             | TypeScript catches most of what the plugin catches; React 19 runtime warnings for keys are surfaced in tests.                      | plugin publishes ESLint 10 support.           |
| Coverage inside workerd     | `@vitest/coverage-v8` cannot instrument the Workers pool (`node:inspector` is a stub in workerd). | Worker modules are unit-tested in Node (`*.test.ts`) where coverage is measured; `*.workers.test.ts` verifies runtime wiring only. | Cloudflare adds coverage support to the pool. |
| `@jsquash/avif` AVIF export | Stretch item; WASM size budget needs measuring.                                                   | PNG/JPEG/WebP exports.                                                                                                             | M5.                                           |

Gate traps found and neutralised during M0 (each would have passed silently):

- **`knip --strict` in knip 6 implies `--production`.** With it, only the two
  entry files were parsed and a deliberately unused `zod` dependency was not
  reported. Confirmed by instrumenting `dist/graph/build.js`: project patterns
  arrived negated and `walkAndAnalyze` ran in 0.1 ms. The gate now runs without
  `--strict` and with `--treat-config-hints-as-errors`.
- **`import-x/no-cycle` is not used.** dpdm is the cycle gate and was verified
  on a real two-module cycle.
- **`gitleaks git` on an empty repository reports "no leaks"** because there is
  no history yet. The gate was proven with `gitleaks dir` and with a staged
  secret under `--pre-commit --staged` before the first commit.

Reasoning for taking ESLint 10 over ESLint 9 with the two React plugins: ESLint's
support policy gives the previous major about six months of maintenance after a
new major; ESLint 10 shipped in early 2026, so starting a new project on 9 in
September 2026 would begin on an end-of-life line. `eslint-plugin-unicorn@74`
also requires ESLint ≥ 10.4.

### 3.4 Repository layout

Single npm package. Three TypeScript projects share one root so that
`src/shared` is type-checked under both the DOM and Workers libraries.

```
watermark-pro/
├── .github/workflows/ci.yml       CI: quality (incl. build) → e2e; semgrep in parallel
├── .husky/pre-commit              gitleaks --pre-commit --staged + lint-staged
├── e2e/                           Playwright specs (+ axe)
├── migrations/                    D1 migrations generated by drizzle-kit (M1)
├── public/                        static assets copied verbatim
├── src/
│   ├── client/                    React SPA (DOM lib)
│   │   ├── routes/                TanStack Router file routes
│   │   ├── components/            UI components
│   │   ├── engine/                watermark engine (pure TS, runs in a Web Worker) (M2)
│   │   ├── lib/                   client utilities
│   │   └── styles/app.css         Tailwind entry + design tokens
│   ├── worker/                    Hono API on Cloudflare Workers (Workers lib)
│   │   ├── index.ts               entry: env validation, middleware, routes
│   │   ├── env.ts                 zod schema for bindings + vars
│   │   ├── db/                    drizzle schema + client (M1)
│   │   ├── auth/                  Better Auth instance + RBAC statements (M1)
│   │   └── routes/                one file per resource (M1)
│   └── shared/                    constants, zod schemas, types (no DOM, no Workers globals)
├── index.html                     Vite entry
├── wrangler.jsonc                 Worker + bindings + assets config
├── vite.config.ts
├── vitest.config.ts               projects: unit-client (jsdom), unit-worker (Node), workers (workerd)
├── vitest.workers.config.ts
├── playwright.config.ts
├── drizzle.config.ts              (M1)
├── tsconfig.json                  references only
├── tsconfig.base.json             strict compiler options
├── tsconfig.client.json / tsconfig.worker.json / tsconfig.node.json
├── eslint.config.mjs, knip.jsonc, .prettierrc.json, .stylelintrc.json, .jscpd.json
├── worker-configuration.d.ts      generated by `wrangler types`; committed
├── PLAN.md, README.md, CHANGELOG.md, SECURITY.md, LICENSE
└── AGENTS.md, CLAUDE.md, .cursor/rules/, .github/copilot-instructions.md
```

### 3.5 Code standards (enforced by the gates above and by the agent instruction files)

- No unexplained magic numbers or strings. Tunables live in `src/shared/constants.ts`
  (the one file where `no-magic-numbers` is off) or in typed config objects.
- No dead code, commented-out code, unused exports, or unused dependencies.
- No silent fallbacks. A function that cannot do its job throws or returns a typed error.
- No mock data outside `*.test.ts`, `e2e/`, and explicitly named fixtures.
- No `any`, no `!`, no `@ts-ignore`. `@ts-expect-error` and rule suppressions need
  an inline reason **and** an entry in §9 of this file.
- Every dependency added must be justified here with version and peer check.

---

## 4. Open questions

| ID  | Question                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Default until answered                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| Q1  | ~~Email provider?~~ **Answered 2026-09-05: Cloudflare.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Cloudflare Email Sending behind a provider interface (A6).                                    |
| Q2  | ~~Is SSO required?~~ **Answered 2026-09-05: no.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Out of scope. Better Auth has an SSO plugin if this changes.                                  |
| Q3  | ~~Retention policy?~~ **Answered 2026-09-05: no auto-delete.**                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Per-org storage quota constant; users delete manually.                                        |
| Q4  | ~~GitHub?~~ **Answered 2026-09-05: yes, public.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | A2 updated.                                                                                   |
| Q5  | ~~Cloudflare plan?~~ **Answered 2026-09-05: Free.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | A13 added; see Q7 and Q8 for the two consequences that need a decision.                       |
| Q6  | ~~Turnstile?~~ **Answered 2026-09-05: yes.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Enabled in M8 behind an env flag; needs site key + secret.                                    |
| Q7  | ~~Email needs a domain.~~ **Resolved 2026-09-05** (`blowmoney.net` zone) but superseded by Q9: the zone is not authorized for Email Sending.                                                                                                                                                                                                                                                                                                                                                                              | —                                                                                             |
| Q9  | ~~Sender domain for Email Sending.~~ **Resolved 2026-09-06.** Email Sending enabled on `watermark.blowmoney.net` (`wrangler email sending enable`); Cloudflare created the `cf-bounce` MX/SPF, DKIM and `_dmarc` records under that subdomain only, so the apex domain's mail is untouched. Sender is `no-reply@watermark.blowmoney.net`; delivery verified against the live Worker. Alternatives considered: the already-authorized `projects.blowmoney.net`, or the apex (would add `_dmarc.blowmoney.net` `p=reject`). | —                                                                                             |
| Q8  | ~~Password hashing on the Free plan.~~ **Resolved 2026-09-05:** the owner upgraded to Workers Paid, so the CPU budget is no longer a constraint.                                                                                                                                                                                                                                                                                                                                                                          | M1 uses Better Auth's default password hash and records its measured CPU cost for the record. |
| Q10 | ~~Tag-driven deploys need `CLOUDFLARE_API_TOKEN` in the repository secrets.~~ **Resolved 2026-09-06.** Token "github-actions watermark-pro deploy" (Workers Scripts, D1, R2, Email Sending: Edit; Account Settings: Read; DNS, Workers Routes, SSL and Certificates: Edit on `blowmoney.net`) created in the dashboard and stored with `CLOUDFLARE_ACCOUNT_ID`; the v1.0.1 Deploy run then passed end to end (gates, migrate, deploy, smoke).                                                                             | —                                                                                             |
| Q11 | ~~Cloud-drive pickers (M16) need vendor app registrations.~~ **Answered 2026-09-06:** the OAuth is user-facing (each user connects their own personal or work Google Drive, OneDrive or Dropbox); the app registrations are made by the implementing agent in Randy's vendor accounts, with the Azure CLI for Microsoft and Chrome Control for Google and Dropbox (`docs/plans/m16-import-and-share.md` part 5). M16 is certified only with all three working.                                                            | —                                                                                             |
| Q12 | ~~Who reviews machine translations (M18)?~~ **Answered 2026-09-06:** nobody; the agent translates, back-translates in a separate pass, compares, retries once, and cuts any language that does not pass (`docs/plans/m18-localisation.md`, "Translation quality process"). **M18 landed 2026-09-08: all twelve languages passed; none cut** (per-language self-QA in `docs/i18n/`, structural enforcement by `catalogues.test.ts`).                                                                                       | —                                                                                             |
| Q13 | ~~May video be dropped if the M17 spike fails?~~ **Answered 2026-09-06: no.** Video ships; the spike only decides codecs and containers per browser (`docs/plans/m17-video-and-pdf.md`, "Codec choice").                                                                                                                                                                                                                                                                                                                  | —                                                                                             |
| Q14 | ~~Observability dashboard toggles (M19).~~ **Withdrawn 2026-09-06:** Workers Logs are already enabled in `wrangler.jsonc`; health checks are a cron trigger in the same file. Nothing needs the dashboard.                                                                                                                                                                                                                                                                                                                | —                                                                                             |

---

## 5. Architecture notes

### 5.1 Request flow

1. Browser loads the SPA from Workers Static Assets. `not_found_handling` is
   `single-page-application`; `run_worker_first` is `["/api/*"]` so only API
   paths invoke the Worker and everything else is served from the asset store.
2. `/api/auth/*` is handled by Better Auth. Sessions are HttpOnly, Secure,
   SameSite=Lax cookies. CSRF protection is Hono's `csrf` middleware plus Better
   Auth's origin checks.
3. `/api/*` routes are Hono handlers. Every handler runs `requireSession` and
   `requirePermission(resource, action)` before touching data. Every body and
   query is parsed with a Zod schema from `src/shared/schemas`.
4. D1 holds metadata (users, orgs, members, watermarks, photos, jobs, share
   links, audit log). R2 holds bytes (originals, outputs, thumbnails, logo
   assets), keyed `org/{orgId}/…` so authorization is a prefix check.
5. R2 objects are never public. Downloads stream through the Worker after an
   authorization check, or through a signed, expiring share token.

### 5.2 Watermark engine (client)

Pure TypeScript module in `src/client/engine/` with no React imports, executed
in a dedicated Web Worker via `OffscreenCanvas`. The pipeline draws through a
small `CanvasBackend` interface (`engine/canvas.ts`) so it also runs on
`HTMLCanvasElement` on the main thread (`engine/local-engine.ts`) where the
worker cannot draw: Safari before 16.4 and Playwright's Windows WebKit build.
`src/client/lib/canvas-backend.ts` picks the path once by capability
detection, never by user agent; both paths are covered by the Chromium
browser tests and the WebKit end-to-end projects. Inputs: `ImageBitmap`,
an ordered list of marks (each a `WatermarkSpec`: text | symbol | image | qr;
font, size, contrast mode, opacity, rotation, tiling, margin, backdrop box,
anchor or explicit box, plus the bitmap or icon path it needs), and render
options. The pipeline runs in one order: orient (quarter turns, flips,
straighten with an auto-crop), crop, resize, colour adjustments (M11), then
analyse, then marks, then encode (`src/client/engine/pipeline.ts`).
Orientation and the crop are one affine mapping the output frame back onto
the source, so the source is drawn once; colour adjustments are pure pixel
maths (`engine/adjust.ts`), and when they are present the placement analysis
reads the adjusted pixels so a mark on a filtered photo still picks legible
ink. Output: `Blob` in the requested format plus one placement and
contrast outcome per mark. Marks are composed in order over the analysed
source; they do not avoid each other, later ones paint over earlier ones, and
each gets its own smart placement and contrast from the same luminance map
(M10). Text tokens (`{date}`, `{time}`, `{filename}`) are resolved by the
caller per photo (`lib/spec-tokens.ts`) before the spec reaches the engine,
so the engine stays pure.

**Smart placement (R4).** For each candidate box (nine anchors plus a coarse
sliding grid), compute on a downscaled luminance map:

- edge density (Sobel magnitude mean) — busy regions are bad hosts;
- local variance and entropy — flat regions read cleanly;
- centre-weighted saliency proxy (distance from image centre and colour
  contrast against the global mean) — avoid the subject;
- a small prior favouring bottom-right, then bottom-left, then top corners,
  matching photographer convention.

The lowest-cost box wins. All weights are named constants; the algorithm is
deterministic and unit-tested against synthetic fixtures with known answers.

**Auto contrast (R5).** Sample mean relative luminance under the chosen box.
If it is above the mid threshold, use the dark variant of the watermark;
otherwise the light variant. Add a soft shadow or outline scaled by how close
the region's luminance is to the watermark's own. Expose the result as the
default position of a contrast slider that the user can override. A third
mode, `colour` (M10), takes any `#rrggbb` ink: the outline, shadow and
backdrop box take the tone opposite to the ink's own luminance, so the same
readability rules hold. QR marks ignore the contrast mode and are always dark
modules on a light field with a quiet zone, because readers expect it.

### 5.3 RBAC model

Organizations are the tenancy boundary. Roles inside an organization:

| Role   | Watermarks | Photos | Jobs | Sharing       | Members            | Org settings  | Audit log |
| ------ | ---------- | ------ | ---- | ------------- | ------------------ | ------------- | --------- |
| owner  | CRUD       | CRUD   | run  | create/revoke | invite/remove/role | update/delete | read      |
| admin  | CRUD       | CRUD   | run  | create/revoke | invite/remove/role | update        | read      |
| editor | CRUD       | CRUD   | run  | create/revoke | —                  | —             | —         |
| viewer | read       | read   | —    | —             | —                  | —             | —         |

Permissions are declared once with Better Auth's `createAccessControl` in
`src/worker/auth/permissions.ts` and enforced by middleware. A platform-level
`admin` role (Better Auth admin plugin) can list, ban, and impersonate users
for support; impersonation is audit-logged.

### 5.4 Security controls

- Secure headers on every response (CSP `default-src 'self'`, `frame-ancestors 'none'`,
  `object-src 'none'`, HSTS, `Referrer-Policy: strict-origin-when-cross-origin`,
  `Permissions-Policy` denying camera/microphone/geolocation).
- Startup validation of every binding and variable with Zod; the Worker refuses
  requests with a 500 and a logged reason if configuration is invalid.
- Upload validation: size limit, MIME sniffed from magic bytes (not trusted from
  the client), dimension limit, EXIF stripped by re-encode on the client.
- Rate limiting on auth endpoints and upload endpoints via the Workers Rate
  Limiting binding.
- Audit log table written for every mutating action (who, what, target, when, IP hash).
- Secrets only via `wrangler secret` / `.dev.vars`; `.dev.vars` and `.env` are git-ignored;
  `.dev.vars.example` documents every variable.
- gitleaks in pre-commit and CI; `npm audit` in CI; semgrep in CI.
- Dependency updates reviewed against peer ranges before merging.

Accepted residual risks (re-evaluated in M8):

- `style-src 'unsafe-inline'` in the static-asset CSP (`public/_headers`). UI
  primitives position popovers with `style` attributes, and CSS Level 3
  `style-src-attr` is not yet universally honoured. Scripts remain strictly
  self-hosted; inline styles cannot exfiltrate data on their own. Tracked in §9.
- `blowmoney.net` hosts other properties besides this app. Cookies are scoped
  to `watermark.blowmoney.net` (no `Domain` attribute), HSTS is set on the
  subdomain only, and nothing served from the apex is trusted by the app.

### 5.5 Performance budgets

| Metric                                                            | Budget                        |
| ----------------------------------------------------------------- | ----------------------------- |
| Initial JS (gzip) for the shell route                             | ≤ 180 kB                      |
| Editor route chunk (gzip)                                         | ≤ 250 kB excluding fonts      |
| Any single font file                                              | ≤ 120 kB (variable, subset)   |
| Lighthouse Performance / Accessibility / Best Practices (desktop) | ≥ 90 / ≥ 95 / ≥ 95            |
| Lighthouse Performance / Accessibility / Best Practices (mobile)  | ≥ 85 / ≥ 95 / ≥ 95            |
| Touch targets on coarse pointers                                  | ≥ 44 × 44 px hit area         |
| Bulk job throughput (4000×3000 JPEG, text watermark, 8 workers)   | ≥ 2 images/s on a 2023 laptop |
| API p95 (D1 read routes)                                          | ≤ 150 ms at the edge          |

---

## 6. Milestones

Every milestone is complete only when its certification checklist is fully
green. No milestone starts before the previous one is certified.

### M0 — Scaffold and gates (this session)

- **Goal:** empty, buildable, fully gated project with documentation.
- **Scope:** configs, gate scripts, minimal SPA + Worker "hello", one unit test,
  one Workers test, one e2e smoke spec, CI workflow, docs, first commit.
- **Files:** see §3.4.
- **Acceptance:** `npm run quality` passes; every gate has been shown to fail on
  a deliberate defect and then pass after revert; `npm run build` produces
  `dist/`; `npm run dev` serves the shell.
- **Certification checklist:**
  - [x] `npm ci` clean, lockfile committed
  - [x] `format:check`, `lint`, `lint:css`, `typecheck`, `deadcode`, `lint:cycles`, `lint:dup`, `security:secrets`, `security:audit`, `security:sast`, `test`, `test:e2e`, `build` all pass
  - [x] each gate proven to fire (table in §8)
  - [x] README commands all executed successfully
  - [x] CHANGELOG has the 0.1.0 entry
  - [x] git initialised, `.gitignore` present before first commit, first commit made

### M1 — Foundation: auth, RBAC, design system — certified 2026-09-06

- **Goal:** users can sign up, sign in, create an organization, invite members with roles, and see an empty dashboard in the final visual language.
- **Scope delivered:** D1 schema + migration; Better Auth integration with email verification and password reset; permission statements (`src/shared/permissions.ts`); `requireSession` / `requirePermission` / `requireSameOrigin` middleware; audit log; rate limiting; email providers (Cloudflare Email Sending, console); app shell (sidebar, organization switcher, account menu, theme toggle, light/dark, responsive); auth screens; dashboard; members management; audit log viewer; organization creation.
- **Tests delivered:** 113 Node/jsdom tests (real Hono + real Better Auth on the memory adapter; router-level page flows with a fake auth client), 5 workerd tests (real D1, real rate-limit bindings), 6 Playwright tests including the full onboarding journey with axe on every page. Coverage 97 % lines / 89 % branches / 93 % functions against 90/85/90 floors.
- **Security checks delivered:** cookie flag test (HttpOnly, SameSite=Lax, Path), CSRF negatives for JSON, form and missing-origin posts, rate limit negative (11th credential attempt → 429 with `X-Retry-After`), CSP header assertions in unit and e2e tests, zero CSP violations in the browser.
- **Docs:** README (install, D1, env table, roles, deployment), SECURITY.md controls, CHANGELOG 0.2.0.
- **Certification checklist:**
  - [x] all gates in §3.2 pass (`npm run quality`, `security:sast`, `test:e2e`)
  - [x] Lighthouse desktop on `/`, `/login`, `/signup`, `/app`, `/app/members`, `/app/audit`: Performance 96–98, Accessibility 100, Best Practices 100 (`docs/lighthouse/m1/summary.md`, uncompressed local preview; production adds brotli)
  - [x] screenshots in light and dark for every screen under `docs/screenshots/m1/`
  - [x] every route has positive and negative RBAC tests (audit route: owner 200, admin 200, editor 403, viewer 403, non-member 403, anonymous 401; invite: viewer 403)
  - [x] bugs found by tests fixed before certification (see §8)
- **Not in M1 (deferred to M8 by design):** production wrangler environment, admin console, Turnstile, actual deployment.

### M2 — Watermark engine — certified 2026-09-06

- **Goal:** deterministic engine that renders text/symbol/image watermarks with smart placement and auto contrast.
- **Scope delivered:** `src/shared/watermark.ts` (Zod spec: text / symbol (glyph or icon) / image marks, placement anchor|smart|custom, contrast auto|manual, style with opacity, rotation, scale, margin, tiling); `src/client/engine/` with `analysis.ts` (Rec. 709 luminance, Sobel, integral images, region statistics), `placement.ts` (nine anchors plus a 3×3 interior grid scored on edge density, variance, centre/contrast saliency and a convention prior), `contrast.ts` (variant and outline from the luminance under the mark), `layout.ts` (size, placement resolution, brick-pattern tiling), `render.ts` (canvas drawing for text, glyphs, icon paths and bitmaps), `encode.ts` (PNG/JPEG/WebP with verified MIME), `pipeline.ts` (crop → resize → analyse → place → draw → encode), `worker.ts` + `worker-client.ts` (typed Web Worker protocol, transferred bitmaps, font loading in the worker).
- **Tests delivered:** 36 engine tests: pure-math tests in Node with synthetic maps (busy-vs-flat placement, convention tie-break, subject avoidance, luminance readings, tiling), plus 13 tests in real Chromium via Vitest browser mode (pixels change only under the mark, ink variant flips with background, smart placement lands on the flat half, crop/resize, tiling, icons, image marks, WebP/JPEG/PNG encoding, worker round trip, worker failure and termination). Engine coverage 100 % lines except the worker thread script, which coverage cannot instrument.
- **Performance:** `docs/benchmarks.md`: 0.15 s per 4000×3000 image on one worker, 22.8 images/s with 8 workers (budget ≥ 2 images/s).
- **Certification checklist:**
  - [x] all gates in §3.2 pass
  - [x] benchmark within budget and recorded
  - [x] engine modules import nothing from React or the DOM (checked by the WebWorker-lib TypeScript project `tsconfig.engine-worker.json`)

### M3 — Watermark library, fonts, and symbols — certified 2026-09-06

- **Goal:** users build and save watermark presets from a wide font and symbol catalogue and uploaded logos.
- **Scope delivered:** D1 tables `watermark` and `asset` (migration `0001_library.sql`); R2 `BUCKET` binding in both wrangler environments; `src/worker/routes/library.ts` with `GET/POST /api/orgs/:orgId/watermarks`, `PUT/DELETE …/watermarks/:id`, `GET/POST …/assets`, `GET …/assets/:id/file`, `DELETE …/assets/:id`, every route behind `requireSession` + `requirePermission` (`watermark` resource: viewers read, editors and above write); upload validation by file signature (`src/worker/uploads.ts`, never the declared MIME), 5 MB limit checked on `Content-Length` and on the body, 50 logos per organization, logos served with `Cache-Control: private` through the Worker, deletion refused with 409 while a preset references the logo; audit entries for every mutation. Client: font catalogue of 51 Fontsource families across sans, serif, display, script and mono (`src/client/fonts/catalogue.ts`, data only) loaded on demand through `import.meta.glob` (`fonts/load.ts`, latin subset, weights 400/700 or the variable `wght` file); symbol catalogue of 8 glyph groups and 70 lucide icons flattened to path data (`src/client/symbols/catalogue.ts`); the designer (`src/client/components/designer/`) with mark / placement / style tabs, font and weight pickers, glyph and icon tiles, logo grid with upload and delete, smart / corner / custom placement, auto / manual contrast, opacity, size, rotation, margin and tiling controls, and a live preview rendered by the engine worker on a bundled sample scene or the user's own photo; library list with kind, placement and contrast summaries; routes `/app/library`, `/app/library/new`, `/app/library/$watermarkId`; "Library" navigation entry.
- **Tests delivered:** 11 Node route tests (CRUD with audit trail, malformed JSON and spec, foreign-logo rejection, sniffing of PNG/GIF/SVG bytes regardless of declared type, declared and actual oversize, malformed forms, the 50-logo quota, delete conflict, viewer / outsider / anonymous / editor matrix), 2 workerd tests over real D1 and R2 (json_extract reference count, R2 metadata round trip, conflict then delete), 7 sniffing and 8 spec-helper unit tests, 9 jsdom page tests through the real router with a fake API (list, delete, empty and error states, viewer gating, full designer flow, draft retention across mark kinds, logo upload including a non-image rejection, conflict on logo delete, edit and read-only view), 5 Chromium tests (`loadFont` registers a family on the page, **every catalogue family and weight loads**, sample scene, preview renderer over text / icon / glyph / logo marks with stale-frame discard and custom subject), and 2 Playwright journeys with axe on the library, the designer in every tab, and the read-only view. Total 234 unit/browser tests + 7 workerd tests + 8 e2e tests; coverage 94.5 % lines / 85.5 % branches / 91.3 % functions.
- **Security checks delivered:** content sniffing test with mismatched declared type, size limits before the body is read, quota, RBAC negatives for every write, same-origin guard unchanged, logo responses private and same-origin only.
- **Performance:** every bundled font file ≤ 120 kB (audit test over 51 packages); Lighthouse desktop `/app/library` 96 / 100 / 96 and `/app/library/new` 95 / 100 / 96 (`docs/lighthouse/m3/summary.md`); designer route chunk 72 kB raw / 21 kB gzip.
- **Docs:** README (library, fonts, symbols, logo limits), CHANGELOG 0.4.0, screenshots under `docs/screenshots/m3/` (library and designer, light and dark).
- **Certification checklist:**
  - [x] all gates in §3.2 pass (`npm run quality`, `security:sast`, `test:e2e`)
  - [x] font budget verified per file by `catalogue.audit.test.ts`
  - [x] Lighthouse desktop on all eight pages within §5.5 budgets
  - [x] screenshots in light and dark under `docs/screenshots/m3/`
  - [x] every library route has positive and negative RBAC tests
  - [x] bugs found by tests fixed before certification (see §8)

### M4 — Editor — certified 2026-09-06

- **Goal:** single-image editor with crop, resize, watermark placement, live preview, contrast slider, and apply-from-library.
- **Scope delivered:** `/app/editor` (optional `?preset=` search, validated with Zod) reachable from the navigation and from every library card. `src/client/editor/geometry.ts` (pure crop and resize arithmetic: clamping, eight aspect presets including the photo's own ratio, handle drags with ratio lock anchored at the opposite corner, minimum crop side, locked and free resize, percentage and long-edge fits, output side limits) and `src/client/editor/state.ts` (document + undo history with `checkpoint` / `set` / `commit` so a drag is one undo step; limit 50). `PreviewRenderer` gained `sourceSize`, `subjectScale`, source-pixel transforms scaled to the preview, and `exportFull` for full-resolution downloads; the engine now reports the mark's box and rotation so handles can be drawn. Components under `src/client/components/editor/`: `MarkOverlay` (drag to move, corner handle to scale, top handle to rotate; arrows, Shift, `+`/`-`, `[`/`]` on the keyboard), `CropOverlay` (eight handles, dimmed surround, thirds grid, arrow keys), watermark panel (preset select, "adjusted for this photo" with revert, placement and style panels shared with the designer), crop panel (aspect presets and numeric fields), resize panel (width/height with proportion lock, 25/50/75 %, fit 1080/2048/4096), export panel (PNG/JPEG/WebP, quality, download). Photos open from disk or by drag-and-drop and never leave the browser; the sample scene is the default subject. Ctrl/Cmd+Z, Ctrl+Shift+Z and Ctrl+Y undo and redo outside form fields.
- **Tests delivered:** 12 pure tests for geometry and history, 8 overlay tests with synthetic pointer and keyboard events at a non-1:1 display scale, 6 editor page tests through the real router (preset from URL, keyboard placement, undo/redo/revert, square crop and locked resize producing the expected transform, PNG export with the transform, photo open/reject/reset, drag-and-drop, tiled marks hiding the frame, export failure, keyboard shortcuts, empty library), 1 download helper test, 2 more Chromium tests (transforms scaled to a half-size preview and full-size export at 640×400 and 2560×1600), and a Playwright journey that places by keyboard and mouse, crops to 1:1, resizes to 50 %, downloads a real PNG and checks its IHDR is 320×320, with axe on every tool. Total 263 unit/browser tests + 7 workerd + 9 e2e; coverage 93.8 % lines / 85.2 % branches / 90.9 % functions.
- **Security checks delivered:** no new server surface; photos and exports stay client-side; downloads use a same-origin object URL released immediately.
- **Performance:** Lighthouse desktop `/app/editor` 95 / 100 / 96 (`docs/lighthouse/m4/summary.md`); preview renders are debounced and stale frames discarded; export re-decodes the original once.
- **Docs:** README (editor section), CHANGELOG 0.5.0, screenshots under `docs/screenshots/m4/` (editor and crop tool, light and dark).
- **Certification checklist:**
  - [x] all gates in §3.2 pass (`npm run quality`, `security:sast`, `test:e2e`)
  - [x] Lighthouse desktop on all nine pages within §5.5 budgets
  - [x] visual check: `docs/screenshots/m4/editor-*.png` and `editor-crop-*.png` reviewed (mark frame with handles, crop frame with thirds grid, panels in both themes)
  - [x] keyboard-only operation of both overlays covered by tests and axe
  - [x] bugs found by tests fixed before certification (see §8)

### M5 — Bulk processing and export — certified 2026-09-06

- **Goal:** apply a preset to many photos with progress, cancel, retry, and download as ZIP in PNG/JPEG/WebP (AVIF stretch).
- **Scope delivered:** `/app/bulk` ("Bulk" in the navigation). `src/client/bulk/queue.ts`: a generic concurrency-limited job queue (submission order, `AbortSignal` cancellation that keeps finished results, retry of failed and cancelled jobs, per-job duration, failure isolation, `onChange` snapshots). `worker-pool.ts`: a pool of engine workers sized to `hardwareConcurrency − 1` (max 8), least-busy dispatch. `processor.ts`: decode on the main thread, optional fit-to-long-edge resize, render and encode in a worker, output naming. `zip.ts`: fflate streaming ZIP with stored (uncompressed) entries and duplicate-name suffixes. `runtime.ts` ties them together behind one factory so the page can be tested with a fake. The bulk tool: multi-file picker and drag-and-drop with de-duplication and a 500-file cap, preset select, format, quality (disabled for PNG), size (original or fit 1080/2048/4096), Start / Cancel / Retry n, a `<progress>` bar with a live summary and throughput, per-file status, error and download, "Download n as ZIP", clear. `MarkResources` was extracted from the preview renderer so the editor, designer and bulk tool resolve fonts, icons and logos the same way. AVIF output was not attempted: Chromium cannot encode it through `canvas.convertToBlob`, and the engine verifies the encoded type, so it stays a stretch goal.
- **Tests delivered:** 5 queue tests in Node with a controllable runner (ordering and concurrency, failure isolation, cancel/retry, a runner that ignores the signal, non-Error rejections and clear); 4 Chromium tests (pool sizing, ZIP round trip through `unzipSync` with duplicate names, processor output naming / WebP and PNG output / long-edge resize / cancellation / undecodable input, and the 20-fixture throughput measurement); 3 page tests through the real router with a fake runtime (batch with a failure, per-file and ZIP downloads, retry; cancel mid-batch with finished results kept, ZIP failure, retry to completion, clear and disposal on unmount; drag-and-drop and the empty-library state); a Playwright journey that uploads twenty generated PNGs, runs the batch with axe on every state, downloads the ZIP and checks all twenty entries in Node. Total 275 unit/browser + 7 workerd + 10 e2e; coverage 93.9 % lines / 85.8 % branches / 91.2 % functions.
- **Performance:** `docs/benchmarks.md`: 20 × 1600×1200 JPEG through 8 workers in 0.18 s = 109.9 images/s end to end (budget ≥ 2 images/s). Lighthouse desktop `/app/bulk` 95 / 100 / 96 (`docs/lighthouse/m5/summary.md`).
- **Security checks delivered:** no new server surface; everything runs in the browser; the ZIP is built in memory from the batch's own outputs and downloaded through a revoked object URL.
- **Docs:** README (bulk section), CHANGELOG 0.6.0, `docs/benchmarks.md`, screenshots under `docs/screenshots/m5/`.
- **Certification checklist:**
  - [x] all gates in §3.2 pass (`npm run quality`, `security:sast`, `test:e2e`)
  - [x] throughput benchmark within budget and recorded
  - [x] Lighthouse desktop on all ten pages within §5.5 budgets
  - [x] screenshots in light and dark under `docs/screenshots/m5/`
  - [x] bugs found by tests fixed before certification (see §8)

### M6 — Storage and gallery — certified 2026-09-06

- **Goal:** watermarked outputs persisted to R2 with thumbnails and metadata, browsable gallery with filters and bulk delete.
- **Scope delivered:** D1 table `photo` (migration `0002_photos.sql`; `preset_id` is `ON DELETE SET NULL` and the preset name is copied so photos outlive their preset). `src/worker/routes/photos.ts`: `GET /api/orgs/:orgId/photos` (newest first, cursor pagination of 60 on `(created_at, id)`, `presetId` filter, case-insensitive `search` with LIKE escaping), `GET …/photos/usage`, `POST …/photos` (multipart `file` + `thumbnail` + name, dimensions, optional preset; declared and actual size limits of 40 MB / 1 MB, signature sniffing for PNG/JPEG/WebP on both files, 10 000 photos and 2 GB per organization checked before any byte is read, unknown preset ids ignored), `GET …/photos/:id/file` and `…/thumbnail` (private cache, inline disposition), `POST …/photos/delete` (up to 200 ids, R2 objects removed first, one audit entry with the count). Client: `lib/thumbnail.ts` (400 px JPEG built in the browser), `lib/gallery.ts` (infinite query, usage query, upload with thumbnail, bulk delete), the gallery page (usage bar, search with deferred value, preset filter, lazy thumbnails, "Load more", per-photo checkboxes with select all, confirm dialog for bulk delete, lightbox with download and delete), "Save to gallery" in the editor's export panel and "Save n to gallery" after a bulk run (sequential uploads, failures counted). `MarkResources`, `formatBytes` and `PresetGate` are shared across tools; "Gallery" in the navigation.
- **Tests delivered:** 6 Node route tests (upload with preset and audit, unknown preset, GIF rejection, missing thumbnail, oversized and declared-oversized, dimension limit, non-multipart, byte quota via seeded records, pagination across 63 photos with a stable cursor, preset filter, search, bad cursor, bulk delete with unknown ids and limits, viewer / anonymous access), 1 workerd test over real D1 and R2 (three uploads, ordering, `%`-escaped search, bulk delete removing objects), 1 Chromium thumbnail test, 4 gallery page tests (usage, paging, filters, lightbox; select and confirm bulk delete plus lightbox delete; viewer restrictions and empty filter; failed load), 2 editor and 1 bulk page tests for saving to the gallery including quota failure, and a Playwright journey (save from the editor, browse, search, filter, open, download and check the PNG, delete, audit) with axe on the gallery and the lightbox. Total 289 unit/browser + 8 workerd + 11 e2e; coverage 94.1 % lines / 86.2 % branches / 91.9 % functions.
- **Security checks delivered:** signature sniffing of both uploaded files, size limits before reading bodies, count and byte quotas, organization-scoped R2 keys, private cache headers, RBAC negatives for upload and delete, audit entries for uploads and deletions. Client-reported dimensions are display-only and bounded (≤ 8192 per side); the Worker never decodes images.
- **Performance:** Lighthouse desktop `/app/gallery` 96 / 100 / 96 (`docs/lighthouse/m6/summary.md`); thumbnails are 400 px JPEGs, list pages are 60 rows with an index on `(organization_id, created_at)`.
- **Docs:** README (gallery section, bindings, limits), CHANGELOG 0.7.0, SECURITY.md, screenshots under `docs/screenshots/m6/`.
- **Certification checklist:**
  - [x] all gates in §3.2 pass (`npm run quality`, `security:sast`, `test:e2e`)
  - [x] quota negative tests (count and bytes) and role tests for every photo route
  - [x] Lighthouse desktop on all eleven pages within §5.5 budgets
  - [x] screenshots in light and dark under `docs/screenshots/m6/`
  - [x] bugs found by tests fixed before certification (see §8)

### M7 — Sharing — certified 2026-09-06

- **Goal:** share individual photos or albums via revocable, expiring links and the Web Share API.
- **Scope delivered:** D1 table `share` (migration `0003_shares.sql`: title, photo ids, `expires_at` in unix seconds with 0 for never, `revoked_at`). `src/worker/share-token.ts`: tokens are `<id>.<expiresAt>.<HMAC-SHA-256>` signed with a key derived from the application secret, so nothing secret is stored, the link can be shown again later, expiry is checked before the database, and revocation is a database check on the id. `src/worker/routes/shares.ts`: `GET/POST /api/orgs/:orgId/shares` and `POST …/shares/:id/revoke` behind the `share` permission (owners, admins and editors; photo ids must belong to the organization, at most 200, expiry 1/7/30 days or never), and the public `GET /api/share/:token`, `…/photos/:photoId/file` and `…/thumbnail`, which carry no session, are rate limited per address through the shared limiter (429 with `Retry-After`), serve only photos inside the share, and answer every refusal (expired, revoked, tampered, unknown, foreign photo) with the same 404. Audit entries `share.created` and `share.revoked`. Client: the gallery's Share button for a selection and the lightbox's Share for one photo open a dialog (title, expiry, Create link, Copy link, Share… through `navigator.share` with clipboard fallback); `/app/shares` lists links with active / expired / revoked status, copy, share and revoke; the public `/share/:token` page (no session, neutral "not available" message) shows the album with thumbnails, a lightbox with download, and a "Share this link" button. The public page is served by the same SPA under the global CSP (`public/_headers`), which already forbids inline and third-party script; a per-route CSP is not possible for a single-page app and not needed.
- **Tests delivered:** 3 token tests (round trip and determinism, expired / tampered id / tampered expiry / tampered signature / extra part / bad expiry / bad id / bad base64 / empty, malformed claims), 5 route tests (publish and anonymous view, file and thumbnail, foreign photo refused, list, audit; unknown photos, empty, oversized, bad expiry and bad JSON; expired, extended-expiry, tampered, unknown and revoked tokens plus double revoke; per-address rate limiting with `Retry-After`; viewer and anonymous access), 1 workerd test over real D1 (create, public view and file, revoke), 7 page tests (create a link for a selection with 30-day expiry and copy it, single-photo never-expiring link from the lightbox, sharing hidden from viewers, shares page statuses / copy / revoke, empty state and viewer block, public album with lightbox and share button, unavailable link), and a Playwright journey (publish from the gallery, visitor in a cookie-less context views and downloads the PNG, cannot reach other routes or a tampered token, owner revokes, visitor is shut out, audit) with axe on the dialog, the shares page and the public page in both states. Total 304 unit/browser + 9 workerd + 12 e2e; coverage 94.0 % lines / 85.6 % branches / 92.2 % functions.
- **Security checks delivered:** HMAC-signed tokens with constant-time verification, expiry in the token and revocation in the database, one 404 for every refusal, per-address rate limiting on public routes, photos served only when listed in the share, `no-store` on the album JSON, RBAC negatives for create / list / revoke, audit entries.
- **Performance:** Lighthouse desktop on the public page 97 / 100 / 96 (`docs/lighthouse/m7/summary.md`); album JSON is one query plus a `findMany` on the listed ids.
- **Docs:** README (sharing section), CHANGELOG 0.8.0, SECURITY.md, screenshots under `docs/screenshots/m7/` (share dialog, shares page, public album, light and dark).
- **Certification checklist:**
  - [x] all gates in §3.2 pass (`npm run quality`, `security:sast`, `test:e2e`)
  - [x] expired / revoked / tampered token negatives and role tests
  - [x] Lighthouse desktop on all twelve pages within §5.5 budgets
  - [x] screenshots in light and dark under `docs/screenshots/m7/`
  - [x] bugs found by tests fixed before certification (see §8)

### M8 — Enterprise hardening and release — certified 2026-09-06

- **Goal:** production deployment at `watermark.blowmoney.net`, admin console, hardening, documentation. (The `production` wrangler environment and the first deployment were pulled forward to 2026-09-06, right after M1; see CHANGELOG.)
- **Scope delivered:** Platform administration: a separate Better Auth `admin` role (never granted through the API; the first administrator is promoted with one D1 update per `docs/runbook.md`), `src/worker/middleware/platform-admin.ts` (`requirePlatformAdmin`), `src/worker/routes/admin.ts` (`GET /api/config` publishing the Turnstile site key, `GET /api/admin/organizations` and `GET /api/admin/audit`), `OrganizationStore.listSummaries()` (grouped D1 query in `src/worker/db/organization-store.ts`; the memory double reads the arrays given to Better Auth's memory adapter) and `PhotoStore.usageByOrganization()`, an `ADMIN_AUDIT_ACTIONS` after-hook that records ban / unban / set-role / remove / revoke-sessions with the administrator as actor, and the `/app/admin` console (`src/client/routes/app/admin.tsx`, `src/client/lib/admin.ts`) with Users (email search over Better Auth's `list-users`, ban with a required reason, unban, make / remove admin, sign out everywhere, `(you)` guard), Organizations (member, photo and storage counts) and Audit trail tabs, shared `AuditTable` component and the `Admin` nav item for administrators only. Bot protection: Better Auth's `captcha` plugin with the `cloudflare-turnstile` provider on `/sign-up/email` and `/request-password-reset`, enabled only when `TURNSTILE_SITE_KEY` and `TURNSTILE_SECRET_KEY` are both present (`env.ts` refuses one without the other), the `Turnstile` widget (`src/client/components/turnstile.tsx`, one script load per page, theme from the app setting, `onToken(null)` on expiry or failure) and the `useCaptcha` hook; sign-up and forgot-password hold their submit until a token exists and send it as `x-captcha-response`; CSP gains `https://challenges.cloudflare.com` in `script-src` and `frame-src`. CSP review: the policy has been enforced (not report-only) since M1 with zero violations on every Lighthouse run, so there is nothing to promote; the one third-party origin is now documented in SECURITY.md and the threat model. Rate limits reviewed: the two bindings (10 / min credentials, 120 / min other auth and public share routes) stay; the runbook records how to tune them. Supply chain and release: `.github/workflows/deploy.yml` deploys from `v*` tags after `quality:ci` and the Playwright suite pass on the tagged commit, `dependency-review` job on pull requests (`fail-on-severity: high`, licence allow-list), a composite `setup-node-gitleaks` action shared by CI and Deploy, and a throwaway `.dev.vars` secret generated per CI run so the preview Worker validates like production. Documents: `docs/threat-model.md` (assets, boundaries, STRIDE table with the test that proves each mitigation, accepted residual risks) and `docs/runbook.md` (deploy, rollback, secrets and rotation, logs, D1 Time Travel, R2, first-admin promotion, incident playbook, rate limits). Audit scripts and the e2e suite promote their throwaway user through `scripts/lib/local-admin.ts` (`wrangler d1 execute --local`), the same operation the runbook prescribes.
- **Tests delivered:** 5 admin route tests (organization listing with counts and RBAC, global audit with the admin actions, `list-users` search, `/api/config` with and without keys), 5 Turnstile route tests (missing token 400, rejected 403, accepted 200, reset protected, sign-in untouched, disabled without keys, siteverify stubbed), 1 workerd test proving the grouped member count against real D1 including a zero-member organization, 6 widget tests (single script load, token and expiry callbacks, stored theme, error callback, script download failure, script without API, unmount mid-load), 3 captcha page tests, 4 admin page tests (refusal keeps its heading, ban / unban / promote / sign out / search, organizations and audit tabs, failed loads), and a Playwright admin journey (refusal before promotion, promotion, search, ban with reason, banned member's session ends and sign-in is refused, unban and sign-in again, organizations, audit trail, anonymous 401) with axe on every state. Totals: 322 unit / browser, 10 workerd, 13 e2e; coverage 94.1 % lines / 85.6 % branches / 92.5 % functions. Semgrep: 0 findings over 301 files (267 rules); `npm audit --audit-level=high` clean (4 moderate, see §9); gitleaks clean over 14 commits.
- **Security checks delivered:** platform role enforced server-side on every admin route and by Better Auth's admin plugin, ban revokes sessions and blocks sign-in, admin actions audited, captcha verified server-side before the auth handler, configuration refuses a half-configured Turnstile, CSP origin allow-list limited to Cloudflare's challenge host, deploys only from gated tags, dependency review on pull requests.
- **Performance:** Lighthouse desktop on all thirteen pages 93–98 / 100 / 96 (`docs/lighthouse/m8/summary.md`); the admin overview is two aggregate queries (organizations joined to members, photos grouped by organization) plus one `list-users` page of 50.
- **Docs:** README (status 1.0.0, platform administrators, bot protection, tag-driven deployment, security), SECURITY.md (Turnstile, platform administration, supply chain, links to the threat model and runbook), `docs/threat-model.md`, `docs/runbook.md`, `.dev.vars.example`, CHANGELOG 1.0.0, screenshots under `docs/screenshots/m8/` (admin users, organizations and audit trail plus every earlier screen, light and dark).
- **Certification checklist:**
  - [x] all gates in §3.2 pass (`npm run quality`, `security:sast`, `test:e2e`)
  - [x] admin RBAC negatives (member 403, anonymous 401), captcha negatives (400 / 403), ban negatives (sign-in refused)
  - [x] Lighthouse desktop on all thirteen pages within §5.5 budgets
  - [x] screenshots in light and dark under `docs/screenshots/m8/`
  - [x] SECURITY.md complete; README deployment section executed end to end (`npm run deploy` from the workstation on 2026-09-06; the tag path needs the two GitHub secrets, see §4)
  - [x] bugs found by tests fixed before certification (see §8)

### M9 — Mobile and Safari certification, red drill — certified 2026-09-06 (with M10)

- **Goal:** the product is as polished and as proven on an iPhone as on a desktop: every journey runs on WebKit and Android emulation, layouts are built for touch and safe areas, Lighthouse has mobile budgets, the visual record covers three widths, and the test suite proves itself against deliberate defects.
- **Why:** M0–M8 certified the desktop only (Lighthouse desktop preset, 1440×900 screenshots, Desktop Chrome e2e). The first WebKit run showed the app did not render at all against the preview (CSP `upgrade-insecure-requests` applies to plain-http localhost in WebKit) and that Playwright's Windows WebKit has no `OffscreenCanvas`, which the engine required outright, as Safari before 16.4 does.
- **Scope:**
  - Engine: `CanvasBackend` abstraction (§5.2) with `OffscreenCanvas` in the worker and `HTMLCanvasElement` on the main thread (`LocalEngine`); analysis samples the source bitmap so no canvas is ever an image source; `FontLoader` shared by both; capability detection in `src/client/lib/canvas-backend.ts`; the bulk pool is one engine when workers cannot draw.
  - Shell: phone layout with a compact top bar (menu, brand, theme, account), a bottom tab bar (Library, Editor, Bulk, Gallery) and a "Menu" sheet (organization switcher, every destination) built on Radix Dialog; safe-area insets on the header, tab bar and sheet; `scroll-padding` so focused fields and tapped tabs stay clear of both bars; theme-colour metas kept in step with the applied theme; `viewport-fit=cover`, apple-touch-icon, web manifest.
  - Editor: preview capped at 42 svh on phones; mark handles keep a 44 px hit area on coarse pointers (`pointer-coarse:`); two-finger pinch to scale, twist to rotate and slide to move, tested with synthetic multi-pointer events.
  - Pages: dashboard shows the four tools (the "what is next" roadmap copy from M1 was stale), landing header and calls to action wrap cleanly at 390 px, scrollable tables are focusable regions (axe `scrollable-region-focusable`).
  - Tests: Playwright projects `desktop-chrome`, `iphone` (WebKit), `ipad` (WebKit), `android` (Chromium); `navigateTo` / `expectNoNavLink` helpers drive whichever navigation the layout shows; a per-test client address fixture so four projects do not share one credential-limit bucket; the platform-admin promotion moved from `wrangler d1 execute --local` (which crashed Miniflare when four journeys ran it concurrently against the serving database) to a console-provider-only `POST /api/dev/promote` next to the dev mailbox, tested in Node (400 / 404 / 200 / 404 without the provider) and in workerd (real D1 update).
  - Gates: `scripts/lighthouse.mjs <milestone> mobile` (Lighthouse's phone emulation, budgets in §5.5), `scripts/screenshots.mjs <milestone> [desktop|phone|tablet|all]`, and the red drill (`npm run test:drill`): `scripts/red-drills.mjs` lists deliberate defects (RBAC, CSRF, rate limits, share tokens, uploads, config, cookies, placement, contrast, crop analysis, engine ownership, capability switch, undo, pinch, thumbnails, theme, shell, four gates, two end-to-end journeys) and `scripts/red-drill.mjs` proves each turns its test red, restores the file and refuses survivors.
  - Security: CSP drops `upgrade-insecure-requests` (HSTS and `'self'` on an https origin already forbid http subresources; documented in `public/_headers`, SECURITY.md and the threat model).
- **Certification checklist:**
  - [x] all gates in §3.2 pass: certified on the same tree as M10, see the M10 checklist for the run
  - [x] red drill: every drill red (23 drills at the M9 cut; 41 in the final run, `docs/red-drill/2026-09-06.md`)
  - [x] Lighthouse desktop 99–100 on every page at the M9 cut; mobile 86–91 on eleven pages, editor 77 and designer 82 below the 85 budget (`docs/lighthouse/m9/{desktop,mobile}/summary.md`); both pages brought within budget in M10 (boot-time preload of the sample scene), see `docs/lighthouse/m10/mobile/summary.md`
  - [x] screenshots in light and dark for desktop, phone and tablet under `docs/screenshots/m9/`, reviewed (the phone library card overflow found later by the Android journey is fixed and re-shot under `m10`)
  - [x] bugs found by tests fixed before certification (see §8)

### M10 — Parity with the market — certified 2026-09-06

- **Goal:** close the table-stakes gaps that `docs/competitor-research.md` (eZy Watermark, Watermarkly, Visual Watermark, iWatermark+, uMark, three Android apps) found on 2026-09-06, and add the differentiators phone users of eZy Watermark expect. Pricing is out of scope: the product stays free.
- **Scope as built, in the planned order:**
  1. **Ink colour.** `contrast` has a third mode, `colour` (`#rrggbb` validated by `HEX_COLOUR_PATTERN`, outline strength). `engine/contrast.ts` resolves every mode to a `fill`, an outline strength and a light/dark `variant` from the ink's own luminance (`hexLuminance`), so the outline, shadow and box take the opposite tone. The style panel offers Auto / Light / Dark / Colour with a native colour input ("Ink colour"); the default ink is the brand violet.
  2. **Multi-line text and a box.** Text marks hold up to four lines (`MAX_TEXT_LINES`; the designer's textarea trims extra line breaks); `render.ts` measures the widest line and lays lines out at 1.15 em pitch, and the aspect used for placement follows the line count. `style.backdrop { enabled, opacity }` draws a rounded box behind text and symbol marks in the tone opposite the ink, with the shadow off while it is drawn ("Box behind the mark", "Box opacity").
  3. **Several marks per photo.** The engine takes an ordered list of marks and returns one placement and contrast outcome each (`ApplyInput.marks`, `ApplyOutput.marks`; §5.2). The editor document holds up to eight layers (`editor/state.ts`, `MAX_LAYERS`), each a preset with its own adjustments; the preset picker adds a layer, the "Layers, bottom to top" list selects the one the handles and panels edit, and every layer is exported, saved, shared and undone together. Bulk takes several presets, applied in the order ticked.
  4. **Snap to grid.** While dragging, the mark's centre snaps to the margin lines, the thirds and the centre within 8 px (`SNAP_DISTANCE_PX`), guide lines are drawn while it is snapped (`data-snap-guide`), and Alt/Option frees the drag. Tested with synthetic pointer events in `overlays.test.tsx`.
  5. **Share sheet.** `lib/share-file.ts` wraps `navigator.canShare({ files })` and `navigator.share`; the editor's export panel and each finished bulk file show a Share button only where the browser can share that output type, and the download stays. A dismissed sheet is not an error.
  6. **Drawn signature.** The designer's logo section opens a pad (`designer/signature-pad.tsx`; four pen widths, undo, clear). Strokes are kept as geometry (`editor/signature.ts`) and exported through the main-thread backend as a transparent PNG at 1024 px on the long side with an 8 % margin, then uploaded as a logo through the existing asset route and selected for the preset.
  7. **Stamps.** Text marks accept `{date}`, `{time}` and `{filename}`; `lib/spec-tokens.ts` resolves them per photo from the file's last-modified time and name before the spec reaches the engine (the sample scene shows `sample-photo`). Presets keep the tokens; the designer preview, the editor and bulk all resolve them.
  8. **QR codes.** A fourth mark kind, `qr` (content up to 512 characters), rendered by `engine/render.ts` from the module matrix of `qrcode-generator` 2.0.4 (§3.1): always dark modules on a light field with a two-module quiet zone, so it scans on any background; the contrast mode is ignored for it. Designer tab "QR code" with a URL-mode input.
  9. **Metadata policy.** Exports are re-encoded from pixels by the canvas: no EXIF, so no GPS, camera or capture time, and the orientation tag is applied to the pixels first. `bulk.browser.test.ts` feeds a JPEG with a hand-built Exif segment (Orientation 6) through the processor and checks the output for the header and the rotated size. Documented in README ("Exports and metadata"), SECURITY.md and the threat model.
  10. **Found on the way.** Every responsive grid has an explicit single base column (`grid-cols-1`): an implicit `auto` column grows to a card's min-content width, and a long nowrap preset description pushed the library page 86 px wider than a Pixel 7, putting the tab bar's tap targets off-screen. `expectAccessible` in the e2e suite now also asserts the page does not scroll sideways (WCAG 1.4.10), on every page of every journey. The editor and designer declare the sample scene in `staticData.preloadImages`, and `main.tsx` preloads it for the matched URL at boot (`lib/preload.ts`), so the largest paint no longer waits for the session and organization fetches.
- **Not built in M10:** invisible/steganographic marks, video, folder watching, brightness adjustments, template file export. Superseded on 2026-09-06: all of them are scheduled in M11–M17 (see the series entry below and `docs/plans/`).
- **Tests added:** contrast and hex-luminance units; pipeline browser tests for colour, multi-line, box, QR and two-mark composition; preview token resolution with a fake engine; snap and layer overlay tests; editor page test for adding, selecting and removing layers; bulk page test for two presets in ticked order; share helper unit test; signature geometry and export tests plus a page test that draws on the pad and saves; the e2e editor, gallery and bulk journeys use a QR preset, two layers, a coloured boxed two-line stamp and two ticked presets. Ten new red drills (`scripts/red-drills.mjs`): layers, colour, line breaks, backdrop, QR field, tokens, snap, share capability, signature padding, bulk preset order.
- **Certification checklist:**
  - [x] all gates in §3.2 pass: `npm run quality` green (58 files, 365 unit and browser tests; 4 files, 11 workerd tests; coverage 94.2 % lines, 85.2 % branches, 92.4 % functions, 94.1 % statements; knip, dpdm, jscpd, gitleaks, `npm audit` clean; production build passes), semgrep 267 rules / 335 files / 0 findings, Playwright 52 of 52 on `desktop-chrome`, `iphone`, `ipad` and `android` (3.7 min)
  - [x] red drill: 41 of 41 red including four end-to-end drills, `docs/red-drill/2026-09-06.md`
  - [x] Lighthouse, median of five runs through the brotli proxy: desktop 99–100 / 100 / 96 on all thirteen pages; mobile 85–93 / 100 / 96 (editor 85, designer 86, everything else 88–93), all within §5.5 (`docs/lighthouse/m10/{desktop,mobile}/summary.md`). The editor sits on the budget line; its first paint waits on the session and organization fetches, which is a shell change for a later milestone, not a tweak
  - [x] screenshots in light and dark for desktop, phone and tablet under `docs/screenshots/m10/`, reviewed
  - [x] bugs found by tests fixed before certification (see §8)

### M11–M19 — the feature series and the performance milestone (planned 2026-09-06)

Randy's direction on 2026-09-06: implement every feature the market research found missing, exceed the competitors, and do the performance work last so nothing is optimised twice. Each milestone has a full specification under `docs/plans/` written for an implementing agent; `docs/plans/README.md` is the runbook (order, the per-milestone loop, repository facts, drill writing, known traps). The entries below are the index and the tick-boxes; the specs are the contract. Certify each before starting the next; bump the minor version per milestone; M19 is 2.0.0. Three rules from Randy govern the series: newer parity research supersedes any older statement in this plan (`docs/plans/README.md`, "Precedence"); every milestone passes a UX check for simplicity ("Simple by default") before certification; and (from 2026-09-06/07) the e2e, Lighthouse and screenshot audits are deferred from M12–M18 to M19, because they cost more wall-clock than the feature code, e2e risks a :5173 collision with Randy's other work, and M19 owns the performance budgets and the visual record. Each milestone still runs `npm run quality`, `security:sast`, and the red drill (whose own e2e entries manage their own web server); it lists the screens and flows it introduced under "Deferred audits" in `docs/plans/m19-performance.md` for M19 to capture and budget in one pass. Never kill a process to free :5173 — it may be Randy's; defer or ask (see §9).

### M11 — Photo adjustments and orientation — certified 2026-09-07 — `docs/plans/m11-photo-adjustments.md`

- **Scope:** rotate/flip/straighten with auto-crop (`Transform.orientation`), brightness/contrast/saturation/warmth/sepia/vignette as pure pixel maths in the engine (`adjustPixels`), eight named filters with thumbnails, an Adjust tab in the editor, a "Photo adjustments" section in bulk; analysis reads the adjusted pixels.
- **Certification checklist:**
  - [x] `npm run quality` green (402 unit and browser tests, 11 workerd tests, coverage 94.2 % lines, 85.3 % branches, 92.6 % functions, 94.1 % statements, build passes); `security:sast` 267 rules / 365 files / 0 findings; `test:e2e` 56 of 56 on desktop-chrome, iphone, ipad and android
  - [x] the seven new drills red; full drill 48 of 48 red (`docs/red-drill/2026-09-06.md`)
  - [x] Lighthouse desktop 99–100 / 100 / 96 and mobile 85–94 / 100 / 96 on all thirteen pages within §5.5 (`docs/lighthouse/m11/`); screenshots incl. the Adjust tab, light and dark, three widths (`docs/screenshots/m11/`), reviewed
  - [x] `adjustPixels` 447 ms for a 12 MP Noir (logged in the browser test); bugs logged in §8
  - [x] UX pass: the three-step path is unchanged; orientation and adjustments sit in the existing Crop and a new Adjust tab; every control has a plain label and a reset; on a phone the Adjust panel shows the filter strip then the sliders, no jargon
  - [x] 1.3.0 tagged, deployed, released (live at watermark.blowmoney.net, 2026-09-07)
- **Note (2026-09-07):** M11 ran the full audit suite. From M12 the e2e, Lighthouse and screenshot audits are deferred to M19 (they cost more than the feature code, e2e risks a :5173 collision, and M19 owns the budgets); see the runbook and §5.5.

### M12 — Mark engine: arc text, letter spacing, effects, shapes, frame, random placement, full icon library, emoji (planned) — `docs/plans/m12-mark-engine.md`

- **Scope:** text `letterSpacing`/`curve`/`effect` (solid, outline, emboss, engrave); a fifth mark kind `shape` (rectangle, rounded, ellipse, line); `Transform.border` frame; placement mode `random` seeded per photo; an Emoji glyph group. **Carried forward** (not shipped in M12, moved to the icon/UX backlog for a later milestone; recorded here so nothing is lost): search over all 1,790 lucide icons, the editor "Shuffle" button that re-seeds a random layout, and the "Any character" free-input glyph. The curated icon catalogue plus the 96-glyph Emoji group covers the common need; the random _mode_ ships, only its one-tap re-roll is deferred.
- **Certification checklist:**
  - [x] gates: `npm run quality` green (433 tests — 422 unit/browser + 11 workerd; coverage 94.0 % statements, 85.24 % branches, 92.5 % functions, 94.0 % lines; build passes). `security:sast` 0 findings (267 rules / 379 files). Full red drill all red incl. six M12 drills — letter spacing, curve, ellipse-as-rectangle, frame offset, random same-seed, random jitter — and the fixed Colour-ink and Bulk-adjustments drills (`docs/red-drill/2026-09-06.md`). e2e deferred to M19.
  - [x] Lighthouse, screenshots and e2e deferred to M19 (Text tab, Shape tab, Frame control and random/jitter placement added to its "Deferred audits" list)
  - [x] UX pass: the three-step path is unchanged; the new controls live in existing tabs (Text effects under the text mark, Shape as a mark kind, Frame in Adjust), each with a plain label and a reset/toggle; no new top-level screen
  - [ ] `/app/library/new` initial JS growth ≤ 5 kB gz — measured at M19 with the other bundle budgets (deferred audits)
  - [x] 1.4.0 tagged, deployed, released (live at watermark.blowmoney.net, 2026-09-07)

### M13 — Metadata: EXIF tokens, keep/strip policy, capture date, DPI (certified 2026-09-07) — `docs/plans/m13-metadata.md`

- **Scope:** `exifr` 7.1.3 reads camera fields in the browser; tokens `{taken}`, `{camera}`, `{lens}`, `{iso}`, `{aperture}`, `{shutter}`, `{focal}`, `{location}`, `{index}`, `{count}`, `{width}`, `{height}`; `{date}` prefers the capture date; export policy strip (default) / keep except location / keep, implemented as in-place edits of the source Exif block (orientation reset to 1, GPS IFD zeroed, pixel dimensions rewritten) written into JPEG (APP1 + JFIF density) and PNG (`eXIf`/`iTXt`/`pHYs`) output; DPI preserved. WebP is always stripped. A byte engine (`src/client/engine/metadata/{crc32,segments,exif-edit,write}.ts`) does the writing; `photo-metadata.ts` reads the fields. PhotoMetadata lives in the shared `src/shared/metadata.ts` (the reader, the token resolver and the byte editors all need it, and shared code cannot import client code — precedence over the spec's file placement).
- **Certification checklist:**
  - [x] gates: `npm run quality` green (471 tests — 460 unit/browser + 11 workerd; coverage 94.0 % statements, 85.25 % branches, 92.7 % functions, 94.0 % lines; dedup clean; build passes). `security:sast` 0 findings (267 rules / 399 files). Non-e2e red drill 58 of 58 red, incl. eight M13 drills — GPS survives keep-except-location, orientation not reset, strip still copies Exif, APP1 before APP0, DPI dropped on strip, PNG CRC not recomputed, `{date}` ignores capture date, empty token left in place (`docs/red-drill/2026-09-07.md`).
  - [x] fuzz test in place and green: 400 random mutations of a JPEG and a PNG fixture through the scanners, none throws (`fuzz.test.ts`).
  - [x] e2e, Lighthouse and screenshots deferred to M19 (Export-tab metadata policy group and the designer token menu added to its "Deferred audits" list).
  - [x] UX pass: the three-step path is unchanged; the metadata policy is a labelled radio group with one-line help under the format in both the editor Export tab and the bulk output settings; tokens are discovered through an "Insert detail" menu, not a jargon dump, and the `{location}` GPS caveat is spelled out in the field hint.
  - [x] 1.5.0 tagged, deployed, released

### M14 — Bulk power: folders, renaming, pause, batch report (certified 2026-09-07) — `docs/plans/m14-bulk-power.md`

- **Scope delivered:** folder input and folder drag-and-drop with the tree preserved in the ZIP (`folders.ts` — `collectImages`, `readEntries`, `zipPath`); output file-name patterns (`names.ts` — `resolveNamePattern`, tokens `{name}`/`{index}`/`{count}`/`{date}`/`{preset}`/`{width}`/`{height}`, sanitised, empty refused); pause/resume on the queue (`JobQueue.pause`/`resume`, `QueueSnapshot.isPaused`); a CSV batch report (`report.ts`, quoted escaping); the photo list capped at 60 rows with "Show all". The bulk job input is typed (`BulkJobInput`) and EXIF is read in a pool of four when files are added.
- **Delivered in 1.6.1** (was carried forward from the M14 core; "if it was planned i want it"): the **per-photo override** and the desktop **watch folder**. The override opens one batch photo in the editor's embedded mode (`Editor` `embedded` prop, `override-dialog.tsx`); "Apply to this photo" mutates that job's `override` and calls `queue.rerun(id)`, "Apply to all photos" reuses the edits across the batch, "Remove override" restores the batch settings, and the row wears a "Custom" badge. The override decision is factored into pure helpers (`marksForJob`/`transformForJob`/`outputSizeForJob`) unit-tested in jsdom. The watch folder (`watch.ts` scanner + `watch-folder.tsx`, File System Access API, desktop only) picks an input and output folder and watermarks new photos into the output while the page is open; it renders nothing where the API is absent. The scanner is tested against in-memory fake handles and the component is driven in jsdom with a stubbed directory picker.
- **Certification checklist:**
  - [x] gates: `npm run quality` green (494 tests — 483 unit/browser + 11 workerd; coverage 94.0 % statements, 85.04 % branches, build passes; dedup clean). `security:sast` 0 findings (267 rules / 406 files). Non-e2e red drill 63 of 63 red, incl. five M14 drills — index not zero-padded, unsafe characters kept, tree flattened in the ZIP, new jobs start while paused, errors with commas break the CSV (`docs/red-drill/2026-09-07.md`). 1.6.1 adds three more — batch presets drawn despite an override, an adjust that skips its re-run, a watch that reprocesses seen files.
  - [x] the photo list renders at most 60 rows with a "Show all" control (a 61-file page test asserts 60 rows then 61 after expanding), so a 500-photo batch stays responsive.
  - [x] e2e, Lighthouse and screenshots deferred to M19 (the folder input, name-pattern field, pause/resume and report added to its "Deferred audits" list).
  - [x] UX pass: the three-step path is unchanged; "Add a folder" sits beside "Add photos", the name pattern shows a live example, Pause/Resume replace Cancel while running, and "Download report" appears with the ZIP button after a batch settles.
  - [x] 1.6.0 tagged, deployed, released; 1.6.1 (override + watch folder) tagged, deployed, released

### M15 — Preset files, logo tools, invisible mark (certified 2026-09-07) — `docs/plans/m15-presets-logos-invisible.md`

- **Scope delivered:** `.wmp.json` preset export/import with logos embedded, validated client-side by the shared `presetFileSchema` (reusing `watermarkSpecSchema`/`presetNameSchema`) and uploaded through the audited asset/watermark routes with a name-collision rename (`src/shared/preset-file.ts`, `src/client/lib/preset-file.ts`, `components/presets/import-dialog.tsx`, library page Export/Import); logo background removal (corner-seeded flood fill with tolerance + one-pixel feather) and transparent-margin trim before upload (`lib/logo-cleanup.ts` pure + `lib/logo-prepare-pipeline.ts` decisions + `designer/logo-prepare.tsx` panel); an invisible LSB mark in PNG exports — a two-phase seeded walk with a CRC, PNG-only (rejected for lossy in `encodeCanvas`), embedded in `applyWatermark` before encode (`engine/invisible.ts` pure, `engine/encode.ts` guard, `engine/pipeline.ts` pass), with editor + bulk toggles (default = workspace name) and a `/app/verify` page plus a gallery "Check a photo" entry (`components/verify/verify-tool.tsx`, `lib/read-invisible.ts`).
- **Built with parallel Opus-4.8 subagents** (Randy's 2026-09-07 multi-milestone directive): the disjoint pure cores (preset-file, logo-cleanup, invisible) and the library/designer UI cluster were built by agents owning non-overlapping files; the integrator wired every shared file (`constants.ts`, `encode.ts`, `pipeline.ts`, editor/bulk/gallery, routes) and certified.
- **Certification checklist:**
  - [x] gates: `npm run quality` green (564 + 11 tests; coverage 93.71 % statements, **85.01 % branches**, build passes; dedup clean; knip clean). `security:sast` 0 findings. Non-e2e red drill green incl. four M15 drills — invisible mark written into a lossy JPEG, corrupted payload read as valid, preset spec imported unvalidated, background removal ignores its tolerance.
  - [x] coverage §9 exclusions added for the canvas-bound `lib/read-invisible.ts` and `designer/logo-prepare.tsx` (pure decisions live in tested `engine/invisible.ts` / `lib/logo-prepare-pipeline.ts`), alongside the existing `engine/render.ts`.
  - [x] Lighthouse and screenshots deferred to M19 (Import dialog, logo-prepare panel, `/app/verify` added to its list).
  - [x] 1.7.0 tagged, deployed, released

### M16 — Import and share surfaces (certified 2026-09-07; autonomous phase 1.8.0, cloud pickers 1.8.1, cloud read+write 1.8.2) — `docs/plans/m16-import-and-share.md`

- **Scope delivered (autonomous phase, 1.8.0):** import from a URL through a Worker proxy with an SSRF policy (`src/worker/url-policy.ts` — https only, no credentials, no IP-literal/decimal/hex or internal hosts, redirects re-checked; `routes/imports.ts` — `photo:['upload']` gated, per-address `IMPORT_RATE_LIMITER` binding, size cap by declared length AND streamed bytes, byte-sniffed, 15 s timeout, audited by host); camera capture on phones (`lib/capture.ts` + `components/import/take-photo-button.tsx`, the `capture` file-input attribute, not getUserMedia — Permissions-Policy `camera=()` correctly left as is); Android Web Share Target (`public/share-target-sw.js` scoped to `/share-target` + `lib/shared-files.ts`); desktop file handling (`lib/launch-files.ts` + `main.tsx` launchQueue); `/privacy` + `/terms` pages generated from SECURITY.md facts, linked from the landing footer. Correctness catches applied: reused `photo:['upload']` (the spec's `photo:['create']` does not exist); extended the existing `/api/config`/`publicConfigSchema` rather than a new route (for the OAuth phase). Built with parallel Opus-4.8 agents on disjoint client files; the integrator owned the worker slice + all shared files + certification.
- **Scope delivered (cloud pickers, 1.8.1):** all three OAuth apps were registered in Randy's vendor accounts by the agent driving the consoles through the chrome-control MCP (Randy authorised this in place of the "held for Randy" plan): Google Cloud project `watermark-pro-508000` (Auth Platform branding, a Web OAuth client, a Picker API key restricted to the Picker API and the `watermark.blowmoney.net` + `localhost:5273` referrers, the `drive.file` scope); a Microsoft Entra SPA app (any tenant + personal accounts, `Files.Read` delegated, SPA redirects `…/oauth/microsoft`); a Dropbox scoped app (Chooser domains `watermark.blowmoney.net` + `localhost`). The five resulting **public** identifiers live under `env.production` in `wrangler.jsonc` (and `.dev.vars` for local dev), reach the browser through `GET /api/config` (`publicConfigSchema` + `src/worker/env.ts`, validated optional), and gate the buttons via `configuredProviders` (`src/client/lib/imports/source.ts`). Pickers: Google Picker + GIS (`lib/imports/google-picker.ts`), Dropbox Chooser drop-in (`dropbox-chooser.ts`), OneDrive through bundled MSAL (`@azure/msal-browser` 5.20.0, §3.1) + Microsoft Graph with an in-app browse dialog (`onedrive.ts` + `components/import/onedrive-dialog.tsx`). Shared: `download.ts` (`toImageFile`), `CloudImportButtons` wired into the editor (first photo) and bulk (all photos); CSP origins + `Cross-Origin-Opener-Policy: same-origin-allow-popups` in `public/_headers` (§9). Built with parallel Opus-4.8 agents on the three disjoint picker modules; the integrator owned the shared menu, wiring, CSP, config plumbing, tests, and certification.
  - **MSAL decision:** bundled as a pinned dependency (Randy's choice), not CDN-loaded — so `script-src` needs no Microsoft origin.
  - **Known follow-ups (not blocking):** the Google app is in **Testing** (owner + test users can use the Picker in production; general availability needs Publishing, which a stale "config incomplete" console flag currently blocks — `drive.file` is non-sensitive so no verification is required once it publishes). Dropbox is in **Development** (the Chooser still works on the allowed domains); the Microsoft app shows an "unverified publisher" consent notice (users can still consent to delegated `Files.Read`).
- **Certification checklist:**
  - [x] autonomous-phase gates: `npm run quality` green (632 + 12 tests; 85.41 % branches; build passes; dedup + knip clean). `security:sast` 0 findings. Non-e2e red drill green incl. four M16 drills — IP-literal host allowed, redirect hop not re-checked, streamed size cap not enforced, fetched bytes not sniffed.
  - [x] Lighthouse and screenshots deferred to M19 (import buttons, `/privacy`, `/terms` added to its list; M19 confirms best practices ≥ 95 once the picker origins land).
  - [x] 1.8.0 (autonomous phase) tagged, deployed, released.
  - [x] cloud read+write (1.8.2): the Google app is **published to production** (any Google account; no verification — `drive.file` is non-sensitive), the OneDrive app was **re-registered on the correct Microsoft account** with `Files.ReadWrite`, and Dropbox gained `files.content.write` + a PKCE redirect. Save-to-cloud writes the watermarked photos into a "Watermark Pro" folder from the editor's export panel (`google-drive-save.ts` reuses the picker's shared `acquireGoogleDriveToken`; `dropbox-save.ts` runs a browser PKCE flow; `onedrive.ts` `saveToOneDrive` PUTs through Graph) and the bulk results block, via the shared `CloudSaveButtons`. CSP `connect-src` gained the write hosts (§9). `npm run quality` green (714 + 12 tests; 85.37 % branches; build passes; dup + knip clean); `security:sast` 0 findings; non-e2e red drill green.
  - [x] cloud pickers (1.8.1): three OAuth apps registered (agent-driven via chrome-control); pickers wired into the editor and bulk; `npm run quality` green (674 + 12 tests; 85.39 % branches; build passes; dedup + knip clean); `security:sast` 0 findings on the new modules; non-e2e red drill green. The SDK/popup glue cannot be unit-tested (jsdom has no vendor SDK), so the picker modules are coverage-excluded (§9) with their pure decisions tested in `download.ts`/`source.ts` and each module's URL/response mappers; a hands-on production check plus Lighthouse/screenshots stay with the M19 audit list.

### M17 — Video and PDF watermarking (planned) — `docs/plans/m17-video-and-pdf.md`

- **Scope:** M17.0 spike (WebCodecs + `mediabunny`, Chromium local and CI, a WebKit probe and Randy's iPhone by hand) that decides codecs and containers per browser, not whether video ships; a Video tool (MP4/WebM/MOV up to 2 GB / 10 min / 4K, fixed placement from a sampled frame, audio passthrough, bitrate ladder, worker-based); a Documents tool (PDF via `pdf-lib`, marks rasterised per page size, tiling, limits).
- **Delivered (1.9.0):** the M17.0 spike proved the WebCodecs + `mediabunny` 1.55.5 pipeline (encode → decode → mark every frame → re-encode, plus an audio round-trip) green in Chromium locally and in CI before any product code; once the real transcoder covered the same ground the throwaway spike test was removed (§8). A **Video** tool (`/app/video`, sidebar + menu, not the phone tab bar) drops one MP4/WebM/MOV, bounds it before decoding (`probe.ts`, `MAX_VIDEO_BYTES`/`MAX_VIDEO_SECONDS`/`MAX_VIDEO_SIDE`), picks the best encodable codec + container up front (`plan.ts` + `capabilities.ts`, shown as "Saves as MP4 (H.264)"), and transcodes in a dedicated Web Worker (`worker.ts` + `worker-client.ts` + `transcode.ts`): the engine's `composeMark` runs on every frame with placement and contrast computed once from the first frame, audio is copied through when it fits the container else re-encoded (AAC/Opus at `AUDIO_REENCODE_BITRATE`) else dropped with a note, progress shows a frame count + ETA, and Cancel resolves with a `CancelledError` while closing every `VideoSample`. A **Documents** tool (`/app/documents`) marks every page of up to 50 PDFs with `pdf-lib`: the ticked layers are rasterised once per distinct page size at `PDF_RASTER_DPI` (`pdf/raster.ts`, canvas), smart placement falls back to a bottom-right anchor over the blank page, and `pdf/watermark-pdf.ts` embeds each raster once, draws it on every page, refuses encrypted PDFs, enforces `MAX_PDF_PAGES`, and keeps the Info dictionary with `Producer`/`ModDate` set. The bulk and documents tools now share `PresetChecklist` + `selectedSpecs`. Built with two parallel Opus-4.8 subagents on the disjoint video and PDF surfaces; the integrator owned the shared constants, the nav/route/chunk/coverage wiring, and certification.
- **Certification checklist:**
  - [x] spike result in §8; `npm run quality` green (coverage 93.79 % stmts / 85.77 % branches / 91.79 % funcs / 93.74 % lines; build emits isolated `video` 363 kB and `pdf` 420 kB chunks); `security:sast` 0 findings over 406 files; the full non-e2e red drill is all red including the seven new M17 drills. Lighthouse, screenshots and the e2e video/documents journeys are deferred to M19 (`/app/video`, `/app/documents` added to its list).
  - [~] the 60 s 1080p transcode-under-2×-real-time timing and the manual iPhone/WebKit encode check are deferred to the M19 performance-and-device audit pass (they need the M19 perf harness and Randy's phone; the pipeline's correctness is proven by the browser transcode tests).
  - [ ] 1.9.0 tagged, deployed, released
- **Carried forward (recorded here):** free-drag "Custom" video placement is not wired — the Video tool offers Smart + the four corners + Center; placement is computed from the first decoded frame rather than the 1 s frame (a single decode pass with a clean frame-close invariant), which satisfies the fixed-placement requirement. Both are noted for a later refinement.

### M18 — Localisation (planned) — `docs/plans/m18-localisation.md`

- **Scope:** `i18next` + `react-i18next`, twelve languages (en, es, de, fr, it, pt-BR, nl, ja, ko, zh-Hans, ru, ar) with a glossary, typed catalogue keys, lazy per-language bundles, saved preference (`user.locale`) and browser detection, RTL through logical Tailwind utilities with a lint guard, locale-aware dates/numbers/sizes and `{date}` stamps; gates: `no-literal-string`, catalogue completeness test, `i18n:check` in `quality`. Translations are produced and checked by the agent (translate, back-translate, compare, retry once, cut the language if it fails). Worker-rendered email text stays English (§4).
- **Delivered (1.10.0):** the whole client is localised into **all twelve** languages — none was cut. Every user-visible string in `src/client/**/*.tsx` comes from the i18next catalogue (`src/client/locales/en/common.json`, 849 keys nested by area, typed via `i18n/resources.d.ts`); the `i18next/no-literal-string` ESLint gate (`eslint-plugin-i18next`, jsx-text-only, brand + `·` exempt, tests exempt) blocks a hard-coded string. Detection is saved preference → `navigator.languages` → English (`i18n/detect.ts`), the choice persists to `localStorage` and, when signed in, to the `user.locale` column via `PATCH /api/me`; `<html lang/dir>` follows the locale and Arabic sets `dir="rtl"`. The eleven non-English catalogues were each produced and self-checked by an Opus-4.8 agent against `locales/GLOSSARY.md`, with plural forms for the locale's `Intl.PluralRules` categories (ja/ko/zh-Hans `other`; de/nl/en `one/other`; es/fr/it/pt-BR add `many`; ru `one/few/many/other`; ar all six). `catalogues.test.ts` enforces, for every locale, en's key set, non-empty values, placeholder/`<Trans>`-tag parity, and complete plural forms; `i18n:check` (`scripts/i18n-extract.mjs`, in `quality`) forbids dead/missing keys. RTL: a committed one-off codemod (`scripts/logical-utilities.mjs`) swapped physical Tailwind utilities for logical ones, the `watermark-rtl/no-physical-utilities` rule keeps them out, directional icons flip with `rtl:-scale-x-100`, and the editor overlays + centred modals keep physical geometry (§9). Built with two serial phases (the string extraction across ~90 components, then eleven parallel translation agents on disjoint locale files) plus the RTL sweep; the integrator owned the i18n runtime, the gates, the completeness test, the glossary and certification.
- **Certification checklist:**
  - [x] gates incl. `i18n:check` and `no-literal-string`; six of the seven M18 drills red (hard-coded string, missing key, dropped placeholder, unsupported saved locale, RTL not applied, physical utility) — the seventh (`{date}` ignores the locale) is deferred with the locale-aware formatting below. Lighthouse and the `en`/`ar` screenshots at three widths are deferred to M19 (added to its list).
  - [x] all twelve languages passed the completeness test (`catalogues.test.ts`, 45 checks) and the per-language self-QA (recorded in `docs/i18n/`); **none cut**.
  - [ ] 1.10.0 tagged, deployed, released
- **Carried forward (deferred to a localisation follow-up, recorded here and in §4):** locale-aware date/number/size formatting (`format-date.ts`, `format-bytes.ts`, `resolveTextTokens` for `{date}`/`{time}`/`{taken}`) still uses the browser default rather than the active locale — changing it churns rendered strings across many tests, so it is a focused follow-up; full **error-message** localisation (`lib/errors.ts` code→`errors.<code>` key, and Worker-rendered verification/invitation email text) stays English for now; the `e2e/locale.spec.ts` journey and the `ar` screenshots go with the M19 audit pass. Attribute strings (`aria-label`/`title`/`placeholder`) were extracted voluntarily during the sweep but are not gate-enforced (the gate is jsx-text-only to avoid flagging SVG attributes). The `ui/sheet.tsx` slide-in keyframe direction is CSS, not a utility, and is not yet mirrored.

### M19 — Performance and production hardening (planned) — `docs/plans/m19-performance.md`

- **Scope:** paint before JavaScript (static skeleton with an inline hashed theme script; pre-rendered public pages); render signed-in pages from a persisted query cache and a single bootstrap request; JavaScript diet with a bundle-budget gate in `quality`; an offline-capable service worker for the editor with an update flow and `Clear-Site-Data` on sign-out; dependency refresh; request ids, client error reporting and a cron health check on the admin console (no dashboard); a11y/security re-audit; competitor re-check with a "Watermark Pro" column in the matrix. Budgets in §5.5 rise to mobile ≥ 90 / desktop ≥ 95 with numeric FCP/LCP/CLS/TBT limits.
- **Certification checklist:**
  - [ ] every raised §5.5 budget met on every page (`docs/lighthouse/m19/`); bundle report under `docs/bundle/m19.md`
  - [ ] offline journey green; update flow checked across two deploys (§8)
  - [ ] competitor re-check done; README status updated
  - [ ] full gates, drills, device matrix, screenshots in `en` and `ar`
  - [ ] 2.0.0 tagged, deployed, released
- **Progress (2026-09-08):** Randy chose the full prerender + boot-split path
  (not relaxed budgets) for the perf work; landing it in small green increments.
  - **§1 first-paint skeleton (done).** `index.html` carries a static,
    theme-correct skeleton inside `#root` (header + brand mark, shimmer content
    blocks, phone tab bar) that paints before JavaScript and is replaced by
    React's first render. An inline theme script mirrors `lib/theme.ts` and sets
    `data-theme` pre-paint so the skeleton never flashes; the
    `inline-script-csp-hash` Vite plugin hashes it into the `_headers`
    `script-src` at build time (verified byte-exact), so the strict CSP keeps no
    `'unsafe-inline'` for scripts. e2e verification (theme-flash drill) lands
    with the §5 gate step (`e2e/performance.spec.ts`).
  - Measurement foundation landed. `vite.config.ts` now emits the client
    manifest (`build.manifest`), and `scripts/lib/bundle-sizes.mjs` computes
    gzip/brotli from the real chunk graph for both `npm run bundle:report`
    (writes `docs/bundle/m19.md`) and `npm run bundle:budget` (enforces the
    §5.5 gzip budgets: `/` ≤ 90 kB, `/app/*` shell ≤ 140 kB, any route chunk
    ≤ 60 kB excluding the media-engine chunks). Verified the gate fires: it
    reports the current over-budget state rather than passing it.
  - **Honest baseline:** initial-load JS shared by every page is **234.8 kB
    gzip / 206.6 kB brotli** (CSS 9.9 kB gzip). Route chunks already pass
    (largest is `app/bulk` at 10.6 kB gzip, well under 60 kB). The `/` budget
    (90 kB) is unreachable by chunk-splitting alone — the boot entry chunk is
    98.7 kB gzip on its own (react-dom + router + query + i18next core) — so it
    needs the §1 prerender / public-entry split, not only the §3 diet. The
    budget gate is therefore **not yet wired into `quality`** (it would fail);
    it is wired once the surfaces are split and under budget.

---

## 7. Definition of done (applies to every milestone)

1. All gates in §3.2 pass locally and in CI.
2. New behaviour has tests that fail without the change.
3. Coverage thresholds hold (lines 90, statements 90, functions 90, branches 85).
4. No new entries in §9 without a linked justification.
5. README, CHANGELOG, and this PLAN are updated in the same change.
6. For UI work: axe passes with zero violations and Lighthouse targets are met.
7. For API work: every route has positive and negative RBAC tests.

---

## 8. Gate verification log

Every gate must be seen to fail on a case it exists to catch.

### M0 (2026-09-05)

| Gate               | Deliberate defect                                                                                                            | Observed failure                                                     | Reverted     |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | ------------ |
| `format:check`     | Unformatted `src/shared/ugly.ts`                                                                                             | exit 1                                                               | yes          |
| `lint`             | `any` parameter + un-awaited promise                                                                                         | `no-explicit-any`, `no-floating-promises`; exit 1                    | yes          |
| `lint:css`         | `@nonsense` at-rule                                                                                                          | `at-rule-no-unknown`; exit 2                                         | yes          |
| `typecheck`        | `const n: number = 'x'`                                                                                                      | TS2322; exit 2                                                       | yes          |
| `deadcode`         | Orphan file, unused export, unused `left-pad` dependency                                                                     | all three listed; exit 1                                             | yes          |
| `lint:cycles`      | `cyc-a.ts` ↔ `cyc-b.ts` imported from the Worker entry                                                                       | cycle listed; exit 1                                                 | yes          |
| `lint:dup`         | Copy of `env.ts` under `src/shared`                                                                                          | 1 clone, 3.1 % over 0 % threshold; exit 1                            | yes          |
| `security:secrets` | Fake AWS-shaped access key (`AKIA` plus 16 random characters, deliberately not the allowlisted example key) in a source file | `gitleaks dir` 1 leak, exit 1; staged `--pre-commit --staged` exit 1 | yes          |
| `security:audit`   | Scratch project with `lodash@4.17.20`                                                                                        | GHSA-35jh-r3h4-6jhm high; exit 1                                     | scratch only |
| `security:sast`    | `eval(code)` in a source file                                                                                                | `javascript.browser.security.eval-detected`; exit 2                  | yes          |
| `test` (assertion) | `expect(1).toBe(2)`                                                                                                          | 1 failed; exit 1                                                     | yes          |
| `test` (coverage)  | 12 uncovered lines in `src/shared/uncovered.ts`                                                                              | lines 87.83 % < 90 %, functions 89.47 % < 90 %; exit 1               | yes          |
| `test:e2e` (axe)   | `<img>` without `alt` in the shell                                                                                           | `image-alt` violation; exit 1                                        | yes          |
| `build`            | Import of a missing module in `main.tsx`                                                                                     | "error during build"; exit 1                                         | yes          |
| Husky pre-commit   | Staged fake secret, `git commit`                                                                                             | gitleaks rejects, commit aborted                                     | yes          |

Bugs the gates caught in M0's own code: `onError` turned the CSRF
`HTTPException` (403) into a 500 (caught by the CSRF unit test); an unused
`AppEnvironment` type and `ApiErrorCode` type (caught by knip); the
`--strict` knip trap itself (caught by the fire test, not by the tool).

### M1 (2026-09-06)

Tests that failed first and drove a fix:

| Defect                                                                                                                                                            | Caught by                                                       | Fix                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| JSON posts from a foreign origin were accepted (Better Auth checks origins only on requests with redirect URLs; Hono's `csrf()` only inspects form content types) | flow test "rejects a JSON sign-in posted from a foreign origin" | `requireSameOrigin` middleware on every state-changing request                            |
| drizzle schema lacked `account.issuer`, required by Better Auth 1.7                                                                                               | first workerd sign-up                                           | schema drift test against `getAuthTables` (proven to fail when `issuer` is made nullable) |
| Better Auth CLI 1.4.21 generated a schema without `issuer`                                                                                                        | manual comparison                                               | CLI dropped; the drift test is the source of truth                                        |
| Switching organizations kept stale queries and parent route context                                                                                               | shell test, e2e journey                                         | `invalidateQueries()` + `router.invalidate()` after `setActive`                           |
| Organization creation left the `/app` context without the new organization                                                                                        | e2e journey                                                     | same reload after create and after accepting an invitation                                |
| `eslint --fix` deleted `as` assertions on `Response.json()` (generic in Cloudflare types), silently turning typed values into `unknown`                           | `no-unsafe-*` errors after the fix pass                         | tests parse responses with Zod; no assertions on `json()`                                 |
| Empty `role="alert"` rendered for every field, confusing screen readers and `findByRole('alert')`                                                                 | page tests                                                      | alert element rendered only when a message exists                                         |
| Zod's `new Function` JIT probe reported as a CSP violation on every page                                                                                          | Lighthouse best-practices + a Playwright CSP probe              | `z.config({ jitless: true })` from the first client import                                |
| Preview on port 4173 was rejected by the same-origin guard                                                                                                        | e2e                                                             | preview and e2e pinned to the `APP_URL` port                                              |
| Lighthouse ran the mobile throttling preset against desktop budgets (scores ~58)                                                                                  | reading the report's `configSettings`                           | desktop config passed explicitly; retries on `NO_NAVSTART`                                |

Gate fire checks in M1: knip flagged `AuditEntryDto`, `AssignableRole`, `fetchHealth` as unused (removed); jscpd flagged three real duplicates (extracted `useAuthMutation`, `EmailField`/`PasswordField`, timestamp column helpers, a shared fake-auth module); semgrep flagged mutable action tags, missing `min-release-age`, and hand-rolled HTML escaping (all fixed); `min-release-age` refused `lucide-react@1.41.0` as too new; the schema drift test was shown to fail on a nullability change.

### M3 (2026-09-06)

Tests that failed first and drove a fix:

| Defect                                                                                                                                              | Caught by                      | Fix                                                                                                                                 |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| Font catalogue carried 102 per-family loader functions; coverage counted each as an uncovered function and the file dominated the functions floor   | coverage gate (74 % functions) | catalogue reduced to data; loaders come from two `import.meta.glob` patterns in `fonts/load.ts`; a Chromium test loads every family |
| Loading a family's `index.css` pulled every subset and weight (200+ font assets in the build)                                                       | build output listing           | latin-only, weight-specific stylesheets (`latin-400.css` / `latin-700.css` / `wght.css`)                                            |
| Tab and radio controls failed axe colour contrast mid-`transition-colors`                                                                           | Playwright axe on the designer | transitions removed from state-driven controls (tabs, choice groups, tiles)                                                         |
| Style panel headings were `h3` under a page with no `h2` before them                                                                                | Playwright axe (heading order) | section headings promoted to `h2`                                                                                                   |
| Two file inputs with the same `accept` list (preview subject and logo upload) were indistinguishable to assistive technology and to the e2e locator | e2e strict-mode violation      | explicit `aria-label` on both inputs                                                                                                |
| axe ran before the lazily loaded route chunk rendered (no `main`, no `h1`) once the bundle grew                                                     | onboarding e2e                 | e2e helpers wait for the page heading before analysing                                                                              |
| `URL` stub in page tests replaced the constructor and broke `new URL()` inside the fake API                                                         | library page tests             | `createObjectURL` / `revokeObjectURL` assigned onto the real `URL`                                                                  |
| Radix `Switch` and `RadioGroup` need `ResizeObserver`, which jsdom lacks                                                                            | designer page tests            | stub in `test-setup.ts`                                                                                                             |
| knip regex entries are compiled with `new RegExp(value)` directly, so the documented `/…/` form never matched                                       | knip configuration hint        | bare pattern `^@fontsource(-variable)?/` with a comment                                                                             |

Gate fire checks in M3: coverage floors refused the first cut (74 % functions, 83.9 % branches) and were satisfied by restructuring rather than lowering; jscpd flagged four real duplicates (shared `parseSaveRequest`, `deleteScoped`, `seedViewerWorkspace`, `TestClient.createOrganization`); knip flagged `FONT_CATEGORY_LABELS`, `DEFAULT_ICON` and three DTO types before they were used; `no-magic-numbers` pushed file signatures into readable Latin-1 strings; `prefer-iterator-to-array` moved the TypeScript `lib` to ES2025 (Iterator helpers are shipped by every supported runtime); axe caught two real accessibility defects listed above.

### M4 (2026-09-06)

Tests that failed first and drove a fix:

| Defect                                                                                                                                         | Caught by                            | Fix                                                                                                |
| ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------- |
| `useElementSize` took a ref object, so an image that mounted after the first effect run was never measured; overlays stayed hidden             | editor page tests (no overlay found) | hook takes the element from a callback ref and (re)measures whenever it changes                    |
| jsdom's `ResizeObserver` stub never reported, so the first measurement depended on a synchronous `setState` in an effect (React compiler lint) | React hooks lint + page tests        | stub delivers one entry on `observe`, as the real API does; the hook relies on that first delivery |
| Page test read the last rendered spec before the preset render replaced the photo-only render (opacity 0) and was flaky under load             | full-suite coverage run              | assertion waits for the preset's opacity                                                           |
| Resize labels used CSS `capitalize` on lowercase text, so the accessible name was "width (px)"                                                 | page test query                      | explicit label text                                                                                |
| Playwright `getByRole('link', { name })` matched both the card title and the "Open … in the editor" link                                       | e2e strict-mode violation            | `exact: true` on card title lookups                                                                |

Gate fire checks in M4: coverage refused the first cut at 84.7 % branches and was satisfied with tests for drag, drop, tiled marks and export failure rather than a lower floor; jscpd flagged the duplicated `image-size` mock factory and the crop setup in the editor page test (extracted to `fake-image-size.ts` and a helper); the React compiler lint refused a synchronous `setState` in an effect; unicorn refused an event handler named `useSample` as a hook-like name.

### M5 (2026-09-06)

Tests that failed first and drove a fix:

| Defect                                                                                                                            | Caught by                  | Fix                                                                        |
| --------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | -------------------------------------------------------------------------- |
| TypeScript narrowed `signal.aborted` to `false` across an `await`, so the post-render cancellation check was flagged as dead code | `no-unnecessary-condition` | `isAborted(signal)` helper read through a function call                    |
| Throughput was computed from `performance.now()` during render (impure under the React compiler)                                  | `react-hooks/purity`       | start and finish times recorded in state when the batch starts and settles |
| The preset loading / error / empty block was duplicated between the editor's watermark panel and the bulk tool                    | jscpd                      | `PresetGate` render-prop component                                         |
| `Array#sort()` without a comparator in tests                                                                                      | unicorn                    | `toSorted` with `localeCompare`                                            |

Gate fire checks in M5: the page test proved cancellation keeps finished outputs and that the runtime is disposed on unmount; the e2e test proved the ZIP contains twenty decodable PNGs of the expected size; `min-release-age` accepted `fflate@0.8.3` (published 2024).

### M6 (2026-09-06)

Tests that failed first and drove a fix:

| Defect                                                                                                | Caught by                    | Fix                                                                       |
| ----------------------------------------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------- |
| `LIKE` search escaped wildcards but had no `ESCAPE` clause, so a search for "100%" matched everything | workerd search test          | `LIKE … ESCAPE '\\'` in raw SQL; memory store mirrors substring semantics |
| Usage summary was split across elements, so screen readers and tests saw fragments                    | gallery page test            | one text node with a test id                                              |
| "Preset" label lookup matched "Preset name" in Playwright                                             | gallery e2e strict mode      | `exact: true`                                                             |
| The success alert's "gallery" link collided with the navigation link of the same name                 | gallery e2e strict mode      | link scoped to the status region                                          |
| `errorCode` helper and owner sign-up fixture duplicated across route tests                            | jscpd                        | `errorCodeOf` and `signUpOwner` in `test-support/client.ts`               |
| `formatBytes` duplicated between the bulk tool and the gallery                                        | lint (magic numbers) + jscpd | `lib/format-bytes.ts`                                                     |

Gate fire checks in M6: knip flagged `StorageUsageDto`, `PhotoListResponse` and `PhotoPresetRef` as unused (removed) and a duplicate constant expression; the quota test proved uploads are refused at the byte limit without touching R2; the viewer test proved uploads and deletes are refused while reads succeed.

### M7 (2026-09-06)

Tests that failed first and drove a fix:

| Defect                                                                                                      | Caught by                    | Fix                                                     |
| ----------------------------------------------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------- |
| The share dialog's title input overrode the `Field` id, so the label pointed at nothing                     | page test (`getByLabelText`) | id comes from `Field`                                   |
| `user-event` installs its own clipboard stub on setup, hiding the test's spy                                | page tests                   | the spy is installed after `userEvent.setup()`          |
| Headless Chromium denies clipboard writes, so "Copy link" reported an error in e2e                          | share e2e                    | clipboard permissions granted in `playwright.config.ts` |
| `getByLabel('Link')` and `getByLabel('Title')` matched "Copy link" and "Preset name"                        | share e2e strict mode        | `exact: true`                                           |
| Download readers, the viewer-join fixture and the newest-first sort were duplicated across specs and stores | jscpd                        | `downloadBytes`, `joinAsMember`, `newestFirst` helpers  |

Post-merge: the first production deploy of M7 left `0003_shares.sql` unapplied because wrangler treated the closed stdin of `npm run deploy` as a declined confirmation and exited 0; the migration was applied by hand and `scripts/deploy.mjs` now runs the apply step with `CI=true` (wrangler skips the prompt) and fails the deploy if `wrangler d1 migrations list --remote` still shows pending migrations, so a Worker can no longer ship ahead of its schema.

Gate fire checks in M7: the token test proved a signature over a different expiry is refused even though it verifies, the route test proved the rate limiter answers 429 with `Retry-After` for a blocked address and 200 for another, and the e2e proved a cookie-less visitor cannot reach organization routes or a tampered token.

### M8 (2026-09-06)

Tests that failed first and drove a fix:

| Defect                                                                                                                                                                                                               | Caught by                                             | Fix                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| The admin overview read organizations and members through Better Auth's adapter, whose drizzle `findMany` defaults to 100 rows, so newer tenants showed zero members                                                 | visual check of `docs/screenshots/m8` (99 tenants)    | `OrganizationStore.listSummaries()` grouped query; workerd test with real D1                                               |
| The "not an administrator" refusal on `/app/admin` had no `h1`                                                                                                                                                       | admin e2e (axe `page-has-heading-one`)                | heading rendered before the refusal; page test asserts it                                                                  |
| `wrangler d1 execute` stderr was discarded when the promotion helper failed                                                                                                                                          | admin e2e                                             | `spawnSync` with piped output surfaced in the thrown error                                                                 |
| The admin journey exceeded Playwright's 30 s budget under parallel load (two sign-ups, a wrangler subprocess, five axe scans)                                                                                        | admin e2e                                             | `test.slow()`                                                                                                              |
| The user-count matcher assumed fewer than a page of users; the local database had 89                                                                                                                                 | admin e2e                                             | matcher accepts the "showing the newest 50" form                                                                           |
| Audit tables, the `requestUrl` fetch helper, the date formatter and the workflow setup steps were duplicated                                                                                                         | jscpd                                                 | `AuditTable`, `request-url.ts`, `format-date.ts`, composite GitHub action                                                  |
| `forgot-password` re-implemented the email field                                                                                                                                                                     | jscpd                                                 | `EmailField`                                                                                                               |
| CI never had a session secret for the preview Worker, so the e2e job would have failed on GitHub                                                                                                                     | review of `deploy.yml`                                | per-run `openssl rand` secret written to `.dev.vars` in both workflows                                                     |
| Coverage: the Turnstile widget was only mocked                                                                                                                                                                       | `test` coverage gate (branches 84.7 % < 85 %)         | `turnstile.test.tsx`; the unmount-mid-load test was proven to fail when the cancel check is removed                        |
| The committed `worker-configuration.d.ts` had been generated with `.dev.vars` present, so it carried `BETTER_AUTH_SECRET`; the first GitHub run (no `.dev.vars`) regenerated without it and the typegen check failed | CI `Verify generated Worker types are current`        | `cf-typegen` passes `--env-file .env.example`, which makes wrangler ignore `.dev.vars`; types are now identical everywhere |
| `src/client/routeTree.gen.ts` was git-ignored, so on a fresh checkout ESLint (`import-x/no-unresolved`) and `tsc` failed before the build could generate it                                                          | CI quality job (second GitHub run)                    | the route tree is committed; CI diffs it after the build, like the Worker types                                            |
| The quality job never installed Playwright's Chromium, which the Vitest `browser` project needs; only the e2e job did                                                                                                | review of `ci.yml` while the second run was in flight | Chromium is installed by the shared composite action, so every job that runs tests is set up identically                   |

Gate fire checks in M8: the Turnstile route tests proved a missing token is refused with 400 and a rejected one with 403 while a good one passes; the admin route test proved an ordinary owner receives 403 from `/api/admin/*`; the widget test was run against a mutant without the `isCancelled` check and failed; the workerd admin test proved a zero-member organization counts as 0 (a bare `count()` over the left join would report 1).

---

### M9 (2026-09-06)

Tests and audits that failed first and drove a fix:

| Defect                                                                                                                                                                                            | Found by                                                                            | Fix                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The app rendered nothing in WebKit against the preview: CSP `upgrade-insecure-requests` upgraded every `http://localhost` asset to https, which WebKit applies to loopback and Chromium exempts   | first iPhone screenshot: a blank page; console `SSL connect error` on every asset   | directive removed (HSTS and https-only source lists cover production); documented in `_headers`, SECURITY.md, threat model                                        |
| The engine required `OffscreenCanvas` outright (`Can't find variable: OffscreenCanvas`), which Safari before 16.4 and Playwright's Windows WebKit lack                                            | designer probe in WebKit                                                            | `CanvasBackend` abstraction, `LocalEngine`, capability detection, deferred module load; browser tests for both paths                                              |
| Nine icon links in the phone header overflowed the viewport, so every page scrolled sideways                                                                                                      | iPhone screenshots                                                                  | bottom tab bar and menu sheet                                                                                                                                     |
| Sticky preview plus fixed tab bar hid the editor's tool tabs on a phone; tapping them landed on the preview                                                                                       | `editor.spec.ts` on `iphone`: "subtree intercepts pointer events"                   | preview no longer sticky; `scroll-padding` for both bars                                                                                                          |
| Scrollable tables were not keyboard-reachable on phones                                                                                                                                           | axe `scrollable-region-focusable` in `onboarding.spec.ts` on `iphone` and `android` | `tabIndex=0`, `role=region`, accessible names on the audit and organization tables                                                                                |
| The lightbox title was squeezed to zero width beside four buttons at 390 px                                                                                                                       | `gallery.spec.ts` on `iphone`: heading "hidden"                                     | actions on their own row                                                                                                                                          |
| The preset title link overflowed its truncated heading, so its centre sat under a neighbouring icon on an iPad                                                                                    | `library.spec.ts` on `ipad`: click intercepted                                      | the link truncates itself                                                                                                                                         |
| Four device projects signing up from one address tripped the 10-per-minute credential limiter                                                                                                     | matrix run: "Check your inbox" never appeared                                       | per-test `cf-connecting-ip` fixture (the limiter itself stays proven by `auth-flow.workers.test.ts`)                                                              |
| Four concurrent `wrangler d1 execute --local` calls against the serving database crashed Miniflare (`fetch failed`), taking every later test with them                                            | matrix run: mass timeouts after the admin journeys                                  | console-provider-only `POST /api/dev/promote`, tested in Node and workerd; the wrangler helper deleted                                                            |
| `getByRole` skips `display: none` elements, so a wait for the hidden sidebar never resolved on phones                                                                                             | matrix run: every WebKit test timed out at 60 s                                     | CSS locator for the attach wait                                                                                                                                   |
| `env.ts` refused a half-configured Turnstile and nothing tested it                                                                                                                                | writing the red drill manifest                                                      | `env.test.ts` case                                                                                                                                                |
| No test failed when viewers could update presets, when a JSON request came from a foreign origin, or when the photo count quota was ignored                                                       | red drill: three survivors on its first run                                         | `library.test.ts`, `index.test.ts`, `photos.test.ts` cases; all three drills red                                                                                  |
| The designer showed the previous mark's frame after switching to a logo mark without a logo                                                                                                       | `library-pages.test.tsx` under the full suite (timing-dependent before)             | the shown result is derived from the current spec                                                                                                                 |
| Lighthouse mobile: 500 kB of uncompressed JS over simulated slow 4G scored 57 to 72; production serves brotli                                                                                     | first mobile audit                                                                  | audits go through `scripts/lib/compressing-proxy.mjs`; the proxy caches immutable assets and compresses off the event loop after its first version skewed timings |
| Lighthouse mobile: thirty 1 kB icon and primitive chunks, a sequential route-chunk request, a serial organization fetch, a 0.16 CLS from the Inter swap on the admin list, a 121 kB manifest icon | mobile audits                                                                       | `ui` chunk group, layout kept in the entry, route chunks preloaded at boot, parallel organization fetch, metric-matched `Inter Fallback`, quantized icons         |
| Lighthouse desktop and mobile results jittered by up to five points with a busy workstation feeding the simulator                                                                                 | repeated audits                                                                     | median of three runs per page (`LIGHTHOUSE_RUNS`)                                                                                                                 |
| Page tests timed out at 5 s under coverage on a busy workstation while every one passed alone                                                                                                     | `npm run quality`                                                                   | 20 s test timeout and a 4 s Testing Library budget for the jsdom project                                                                                          |
| semgrep: the drill runner spawned commands through a shell                                                                                                                                        | `security:sast`                                                                     | Node entry files run with `process.execPath`, no shell                                                                                                            |

---

### M10 (2026-09-06)

Tests and drills that failed first and drove a fix:

| Defect                                                                                                                                                                                                                                                              | Found by                                                           | Fix                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The bulk page test ticked one preset, so a batch that silently dropped every preset but the first passed it                                                                                                                                                         | red drill "Bulk: only the first ticked preset is applied" survived | the test seeds two presets, ticks them in reverse library order and asserts both specs reach the runtime in ticked order; drill red                                                                                                   |
| The signature pad's page test drew strokes at `NaN` because jsdom reports zero-size rectangles, and the pad would have saved an empty logo                                                                                                                          | `library-pages.test.tsx` (signature)                               | the test stubs `getBoundingClientRect`; `exportSignature` refuses an empty drawing ("draw something first")                                                                                                                           |
| Loading the main-thread engine module eagerly for capability detection pulled the whole pipeline into the entry chunk and cost LCP on every engine page                                                                                                             | Lighthouse mobile after the `LocalEngine` change                   | `DeferredLocalEngine` imports `engine/local-engine` on first use; browser tests cover the deferred path                                                                                                                               |
| Multi-line text was placed with a single-line aspect, so a four-line mark overflowed its box                                                                                                                                                                        | pipeline browser test for line breaks (written before the fix)     | `measureAspect` divides the widest line by the line count at the 1.15 em pitch                                                                                                                                                        |
| The bulk fixture in `bulk.spec.ts` had its `\n` mangled into a literal line break by the shell that wrote it, so the two-line stamp was a one-line stamp                                                                                                            | reading the diff                                                   | rewritten through the editor; the drill manifest got the same treatment (`String.raw`)                                                                                                                                                |
| `placement.test.ts` still expected the M9 shape of a resolved contrast (no `fill`), so the first full `npm run quality` failed one test that had passed alone                                                                                                       | `npm run quality`                                                  | expectation updated                                                                                                                                                                                                                   |
| The library page overflowed a Pixel 7 by 86 px with two long preset descriptions (implicit `auto` grid column under a nowrap description), so the Android bulk and editor journeys could not tap the tab bar or the editor link                                     | Playwright `android` project: "`<main>` intercepts pointer events" | `grid-cols-1` on every responsive grid; `expectAccessible` asserts no horizontal scroll; drill "E2E (Android): library cards overflow the phone width"                                                                                |
| The editor journey counted every button in the layer list (select and remove per layer), so the two-layer assertion expected 2 and found 4                                                                                                                          | Playwright, all four projects                                      | counts list items                                                                                                                                                                                                                     |
| Lighthouse mobile: editor 81 and designer 83 against the 85 budget; the sample scene (the largest paint) was only requested after the session, organization and role fetches                                                                                        | `npm run audit:lighthouse m10 mobile`                              | routes declare `staticData.preloadImages`, boot preloads them for the matched URL; editor 85, designer 86; the editor journey asserts the preload link on a cold load; drill "E2E: editor no longer preloads its first paint at boot" |
| The M9 drill "Main-thread engine: bitmaps never released" had gone stale: the engine now releases bitmaps through `closeInputBitmaps`                                                                                                                               | full drill run ("the drill is stale")                              | drill rewritten against the current line; a one-off check confirmed every other fragment is current                                                                                                                                   |
| The tsc gate drill errored instead of going red: tsc coloured its output despite `FORCE_COLOR=0`, and the escape codes split "error" from "TS2322" so the failure pattern missed                                                                                    | full drill run                                                     | the runner strips VT control sequences before matching                                                                                                                                                                                |
| The drill report was stamped with the UTC date, so an evening run landed on tomorrow's file                                                                                                                                                                         | full drill run at 17:45 local                                      | the runner stamps the local date                                                                                                                                                                                                      |
| The first CI run with the WebKit projects timed out on the iPhone bulk journey and needed two retries on the iPhone editor journey: four browsers on GitHub's two-core runner; and the failure artefact was empty because the GitHub reporter writes no HTML report | CI run 34071727792                                                 | two Playwright workers under CI (52 of 52 in 3.4 min, no retries, against 5.9 min with four); the job uploads `test-results/` on failure                                                                                              |

Gate fire checks in M10: every one of the ten new drills was run alone and seen red before the full drill (see `docs/red-drill/`); the metadata test was run against the untagged fixture first to prove the Exif detector reports false on a clean file and true on the tagged one before the processor is involved; the overflow assertion was run by hand against the un-fixed grid and failed with "page must not scroll horizontally, received 86" before it was wired into the drill; the preload drill was run alone and seen red.

---

### M11 (2026-09-07)

Tests and findings that drove a fix:

| Defect                                                                                                                                                                                                                                                                                                                                                                                       | Found by                                                                                     | Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Branch coverage fell to 84.6 % (threshold 85): the new engine files add defensive `?? 0` reads (from `noUncheckedIndexedAccess`) and a browser-only thumbnail path that jsdom cannot instrument                                                                                                                                                                                              | `npm run test` coverage gate                                                                 | pixel reads routed through the existing `analysis.ts` `read()` helper (one shared branch, not many inline ones); `analysePixels` unit test; an `AdjustPanel` test that mocks the thumbnail renderer; back to 85.3 %                                                                                                                                                                                                                                                                                                                  |
| `adjustPixels` timing test flaked at 2093 ms against a 1000 ms guard, then a CI runner logged 6220 ms against the raised 6000 ms guard, both under the full quality gate (all projects plus coverage saturating the machine)                                                                                                                                                                 | `npm run quality`; CI run 34184375214                                                        | the real time is ~250–450 ms for a 12 MP Noir (logged); the guard is 12 000 ms (~48x real) so a genuine ~2x algorithmic regression still clears it even under the worst observed CI saturation, while scheduling noise does not                                                                                                                                                                                                                                                                                                      |
| The M9 drill "Engine: analysis ignores the crop" went stale: the pipeline was rewritten so crop, resize and orientation are one affine (`outputGeometry`)                                                                                                                                                                                                                                    | full drill run ("the drill is stale")                                                        | drill retargeted to make `analyseSource` drop the transform; confirmed red                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| The iPad WebKit end-to-end project received another local project's app ("Project Map") because that project's dev server holds IPv6 `localhost:5173` while the preview binds IPv4                                                                                                                                                                                                           | matrix run: iPad `toHaveText` mismatches                                                     | not a product defect and not this project's server to touch; desktop-chrome, iPhone and Android pass locally, and CI certifies all four devices in a clean container                                                                                                                                                                                                                                                                                                                                                                 |
| `page.goto('/accept-invitation/…')` threw `page.goto: Frame load interrupted` on the iPhone (WebKit) e2e project only: WebKit aborts the document `load` event while TanStack Router resolves the route on the client (session query + invitation loader), and Playwright's default `waitUntil: 'load'` surfaces that abort as an error                                                      | CI run 34184862736 (`library.spec.ts:117`, iPhone), reproduced locally on the iPhone project | the two accept-invitation navigations (`library.spec.ts`, `onboarding.spec.ts`) wait for `waitUntil: 'commit'` instead of `load`; the following button/heading locators auto-wait for the rendered page, so behaviour coverage is unchanged. Verified green on the iPhone project locally                                                                                                                                                                                                                                            |
| The `auth-flow.workers.test.ts` rate-limit test asserted the `(max + 1)`th wrong-password attempt is exactly the one that returns 429; a CI runner returned 401 there instead (`expected 401 to be 429`). Cloudflare's Rate Limiting binding is approximate and may let a small burst past the limit before its counter catches up, so pinning the trip to a single exact attempt is fragile | CI runs 34185951170 and 34203414726 (`workers` project)                                      | the `max + 5` margin still flaked (a slow runner let 15+ through, "rate limiting did not engage within 15 attempts"), so the test now attempts up to a generous cap (`5 × max`) and asserts only the invariants that always hold: no wrong-password attempt ever authenticates (every pre-throttle response is 401), the first attempts are not throttled instantly, and a 429 with a numeric `x-retry-after` does engage. The exact trip point is not asserted, matching the binding's approximate, eventually-consistent behaviour |

Gate fire checks in M11: each of the seven new drills (contrast ignored, analysis reads the unadjusted photo, turns do not swap width and height, straighten leaves empty corners, rotating keeps a stale crop, adjustments not passed to the runtime, Vivid maps to the wrong values) was run alone and seen red before the full drill; the orientation matrix was verified in `orient.test.ts` against hand-derived corner points, not against the implementation.

M17.0 spike (2026-09-07): before any product code, a Chromium browser test encoded a 3-second clip through WebCodecs + `mediabunny` 1.55.5, read it back, drew a text mark on every frame with the engine's `composeMark`, re-encoded, and asserted 90 frames plus a mark region that changed far more than a codec-noise control region (so a no-op mark would have failed), and a separate case proved an audio track survived a mux/demux round-trip; the encodable codec the browser chose was logged. It passed locally and in CI. Once `transcode.browser.test.ts` covered the same pipeline the throwaway spike test was deleted (it was never committed). Gate fire checks in M17: the seven new drills (video — limits checked after decoding, frames never closed, mark drawn on the first frame only, audio dropped; PDF — last page unmarked, page cap ignored, smart placement used on documents) were each seen red before the full drill. The 60-second 1080p transcode-under-2×-real-time timing and the manual iPhone/WebKit encode check are deferred to the M19 performance-and-device audit pass.

## 9. Tracked escape hatches

Every suppression, `@ts-expect-error`, cast, or config loosening. Empty is the goal.

| Location                                                        | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Remove when                                                                                                   |
| --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `public/_headers`                                               | `style-src 'unsafe-inline'`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | UI primitives set inline `style` attributes; see §5.4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Radix/Base UI ship nonce or CSS-only positioning, or `style-src-attr` is universally supported.               |
| `eslint.config.mjs`                                             | `unicorn/prevent-abbreviations` and `unicorn/name-replacements` off                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Cloudflare (`env`) and React (`props`) vocabulary is short by API design; the rules generate churn against vendor names.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Never; documented choice.                                                                                     |
| `eslint.config.mjs`                                             | `unicorn/single-line-block-comment-style` off                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | One-line `/** */` is still JSDoc and feeds editor hover text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Never; documented choice.                                                                                     |
| `eslint.config.mjs`                                             | `unicorn/prefer-global-this` off for `src/client/**`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `globalThis` lacks `Window` members in TypeScript; obeying the rule is a hard type error in browser code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | TypeScript types `globalThis` as `Window` in DOM projects.                                                    |
| `eslint.config.mjs`                                             | `unicorn/filename-case` and `react-refresh/only-export-components` off for `src/client/routes/**`                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | TanStack Router file naming (`__root.tsx`, `$id.tsx`) and the required `Route` export.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Router changes its conventions.                                                                               |
| `eslint.config.mjs`                                             | `no-magic-numbers` off for `src/shared/constants.ts`, tests, e2e, and `*.config.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Constants must be assigned literals; assertion values are the meaning; config tables read better inline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Never; documented choice.                                                                                     |
| `eslint.config.mjs`                                             | `@typescript-eslint/no-confusing-void-expression` with `ignoreArrowShorthand`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `onClick={() => setOpen(true)}` is the React handler idiom; the other cases of the rule stay on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Never; documented choice.                                                                                     |
| `eslint.config.mjs`                                             | `@typescript-eslint/only-throw-error` allows `Response`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | TanStack Router signals redirects by throwing `Response` objects.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Router offers a non-throwing redirect API.                                                                    |
| `eslint.config.mjs`                                             | test files, `test-support/**`, `e2e/**`, `scripts/**` may use magic numbers, non-null assertions, `beforeEach` fixture assignment, devDependencies                                                                                                                                                                                                                                                                                                                                                                                                                      | Test and tooling idioms; production code keeps the strict set.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —                                                                                                             |
| `eslint.config.mjs`                                             | `import-x/core-modules: ['cloudflare:workers', 'cloudflare:test']` and knip `ignoreDependencies: ['cloudflare']`                                                                                                                                                                                                                                                                                                                                                                                                                                                        | workerd virtual modules have no package on disk.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | knip/import-x learn the `cloudflare:` scheme.                                                                 |
| `tsconfig.base.json`                                            | `skipLibCheck: true`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Type-checking third-party `.d.ts` is slow and fails on bugs we cannot fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Never; documented choice.                                                                                     |
| `vitest.config.ts`                                              | coverage excludes `main.tsx`, `test-setup.ts`, `routeTree.gen.ts`, `zod-config.ts`, `src/worker/db/**`, `src/worker/services.ts`, `src/client/lib/auth-client.ts`, `src/client/engine/worker.ts`, `src/client/engine/render.ts`, `read-invisible.ts`, `designer/logo-prepare.tsx`, `lib/imports/{google-picker,google-drive-save,dropbox-chooser,dropbox-save,onedrive}.ts`, `import/onedrive-dialog.tsx`, `video/{frame,capabilities,probe,transcode,worker,worker-client}.ts`, `components/video/video-tool.tsx`, `pdf/raster.ts`, `i18n/index.ts`, `test-support/**` | DOM bootstrap and generated code; D1 and binding wiring only runs in workerd (verified by the `workers` project, which cannot report coverage); the auth client module is replaced by a fake in page tests and exercised by Playwright; `worker.ts` runs in a Web Worker thread and `render.ts` is pure 2D-canvas drawing (jsdom's `getContext` is null) — both are exercised end to end by the `browser` project (Chromium pixel tests), which cannot report coverage. The M16 cloud pickers load a third-party SDK (Google Picker + GIS, the Dropbox Chooser drop-in) or the bundled MSAL client and drive a vendor popup/iframe that jsdom cannot run; their pure decisions live in tested helpers (`lib/imports/download.ts`, `lib/imports/source.ts`, and the URL/response mappers exported from each module). The M17 video modules run under WebCodecs/mediabunny/OffscreenCanvas and a Web Worker, and `pdf/raster.ts` draws the mark on a page-sized OffscreenCanvas — none instrumentable; their pure decisions live in the tested `video/plan.ts` (and `video/errors.ts`) and `pdf/raster-layout.ts`, and the canvas/worker paths are exercised by the `browser` project and page tests. The M18 `i18n/index.ts` is i18next-instance boot, code-split catalogue loads and `<html lang/dir>` mutation; its locale-selection logic lives in the fully tested `i18n/detect.ts`, and test-setup.ts initialises the instance for every page test. | Cloudflare adds coverage to the Workers pool; a headless-canvas coverage path exists for the browser project. |
| `src/worker/test-support/*.ts`                                  | `{} as never` binding placeholders and one `as unknown as` for the migration binding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Test-only stand-ins for Cloudflare bindings the memory adapter never touches.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —                                                                                                             |
| `src/client/lib/zod-config.ts`                                  | Zod `jitless: true`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Avoids the `new Function` probe that strict CSP reports; costs the JIT fast path on a client that parses tiny payloads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Zod stops probing under CSP.                                                                                  |
| `src/client/video/worker.ts`, `transcode.browser.test.ts` (M17) | one `self as unknown as VideoWorkerScope` cast; two `@typescript-eslint/unbound-method` disables in the test                                                                                                                                                                                                                                                                                                                                                                                                                                                            | mediabunny's public types reference DOM-only types (`HTMLCanvasElement`), so the video worker is type-checked under the DOM lib, not the WebWorker lib the engine worker uses; the dedicated-worker globals are reached through one asserted scope, as `main.tsx` does for the launch queue. The test disables capture `VideoSampleSink.prototype.samples` and `Output.prototype.cancel` to spy on frame-closing and cancellation, each re-invoked with `.apply(this)`. No `any`/`!`/`@ts-ignore`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | mediabunny publishes WebWorker-lib-safe types.                                                                |
| `wrangler.jsonc`                                                | `compatibility_date` held at 2026-08-22                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Newest date the Workers pool's workerd accepts (pool 0.22.0).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Pool ships a newer workerd; bump both.                                                                        |
| `eslint.config.mjs`                                             | `no-magic-numbers` off for `src/client/**/catalogue.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Font and symbol catalogues are literal data tables; the numbers (font weights) are the data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Never; documented choice.                                                                                     |
| `eslint.config.mjs`                                             | `no-magic-numbers` off for `src/client/engine/metadata/**`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Byte-format code (JPEG segments, PNG chunks, TIFF/Exif edits): the marker bytes, chunk-type codes and field offsets are the file format itself; naming every `+2`/`+4` offset would obscure rather than clarify. Same rationale as `catalogue.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Never; documented choice.                                                                                     |
| `segments.ts`, `exif-fixtures.ts`                               | `eslint-disable unicorn/prefer-https` on the XMP APP1 identifier                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The XMP segment identifier is literally `http://ns.adobe.com/xap/1.0/\0`, an XML namespace fixed by the Adobe XMP spec (not a fetched URL); https would not match the bytes cameras write.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Never; the format defines it.                                                                                 |
| `src/client/engine/metadata/write.ts`                           | `new Blob([bytes] as BlobPart[])`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `Uint8Array<ArrayBufferLike>` is a valid `BlobPart` at runtime but not under the lib.dom generic in TS 6; the cast bridges it, as in `zip.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | lib.dom widens `BlobPart` to accept `ArrayBufferLike`-backed views.                                           |
| `knip.jsonc`                                                    | `ignoreDependencies` pattern for `@fontsource/*` and `@fontsource-variable/*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Fonts are reached through `import.meta.glob`, which knip cannot follow; `catalogue.audit.test.ts` checks every catalogue entry against the installed package and `preview.browser.test.ts` loads each one, so an unused package still fails a gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | knip follows `import.meta.glob`.                                                                              |
| `package.json`                                                  | `quality` omits `security:sast`, `test:e2e`, and the audits                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | They need machine tools (semgrep, browsers, Chrome); CI runs sast and e2e unconditionally, audits run at UI milestones.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —                                                                                                             |
| `public/_headers`                                               | `script-src` and `frame-src` allow `https://challenges.cloudflare.com`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Cloudflare Turnstile loads its widget script and challenge frame from that origin; there is no self-hosted alternative. Only that host, only those two directives.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Turnstile is dropped or Cloudflare serves it from the zone.                                                   |
| `public/_headers`                                               | `script-src` allows `https://static.cloudflareinsights.com`, `connect-src` allows `https://cloudflareinsights.com`                                                                                                                                                                                                                                                                                                                                                                                                                                                      | The `blowmoney.net` zone has Cloudflare Web Analytics "automatic setup" on, which injects an SRI-pinned, cookie-less beacon into every HTML response; without the allowance it is blocked and logs a CSP error on every page. Turning the injection off is a dashboard setting on the zone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Automatic setup is disabled on the zone; then remove both entries.                                            |
| `public/_headers`                                               | `script-src`/`frame-src`/`connect-src` allow the M16 cloud-import picker origins: Google (`apis.google.com`, `accounts.google.com`, `docs.google.com`, `www.googleapis.com`), Dropbox (`www.dropbox.com`, `*.dropboxusercontent.com`, and for save `api.dropboxapi.com` + `content.dropboxapi.com`), OneDrive (`login.microsoftonline.com`, `graph.microsoft.com`, and the download/upload hosts `*.files.1drv.com`, `*.up.1drv.com`, `my.microsoftpersonalcontent.com`, `*.sharepoint.com`, `*-my.sharepoint.com`)                                                     | Each picker loads its vendor SDK and downloads the chosen files from these hosts; there is no self-hosted alternative. A picker is offered only when its keys are configured, but the CSP allowances are static. Origins move with `src/shared/constants.ts` and the per-provider modules.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | A picker is dropped, or Cloudflare serves the SDK from the zone.                                              |
| `src/client/lib/imports/dropbox-save.ts`                        | one `eslint-disable-next-line unicorn/prefer-code-point` on the `Dropbox-API-Arg` escaper                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | The header value must be ASCII, and the escaper walks UTF-16 **code units** on purpose so a surrogate pair becomes the two `\uXXXX` escapes JSON understands; `codePointAt` would emit one out-of-range code point and break the escape.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Never; the byte-exact escaping is the point.                                                                  |
| `public/_headers`                                               | `Cross-Origin-Opener-Policy` relaxed from `same-origin` to `same-origin-allow-popups`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | All three M16 pickers hand their result back through a popup's `window.opener` (Google GIS token, Dropbox Chooser, MSAL sign-in); a bare `same-origin` COOP nulls the opener and the picker hangs. `allow-popups` still blocks a cross-origin openee from referencing this window.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | The pickers move to a non-popup flow, or are removed.                                                         |
| `npm audit`                                                     | 4 moderate advisories accepted (esbuild GHSA-67mh-4wv8-2f99 via `drizzle-kit`'s bundled esbuild)                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Development-only dependency; the advisory concerns esbuild's dev server accepting cross-origin requests, which this project never runs. The gate is `--audit-level=high`; the review action fails pull requests at high.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `drizzle-kit` ships a patched esbuild.                                                                        |
| `src/client/bulk/folders.ts`, `bulk-tool.tsx`                   | `file as { webkitRelativePath?: string }` and `event.dataTransfer.items as DataTransferItemList \| undefined`                                                                                                                                                                                                                                                                                                                                                                                                                                                           | lib.dom types both as always present, but `webkitRelativePath` is absent in jsdom and `items`/`webkitGetAsEntry` are absent on browsers without folder-drop support; the casts let the runtime-optional value be checked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | lib.dom types these File System / drag APIs as optional.                                                      |
| `eslint.config.mjs` (M18 RTL)                                   | `watermark-rtl/no-physical-utilities` off for the editor overlays (`crop-overlay.tsx`, `mark-overlay.tsx`), for any line carrying a `physical: geometry` comment (the seven centred modals' `left-1/2`), and for `src/client/**/*.test.tsx`                                                                                                                                                                                                                                                                                                                             | The overlays position handles by literal pointer/keyboard pixel geometry, and the centred modals' `left-1/2` pairs with a physical `-translate-x-1/2` — neither pointer coordinates nor `translate` are mirrored by `dir`, so both keep the physical utility and centre correctly in LTR and RTL. Test `.tsx` files assert on the physical class strings a component renders (including the overlays' geometry), so the shipped-component gate does not apply to them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Never for the geometry cases; the test exemption goes if the rule is taught to skip assertion strings.        |
