save-exact=true
engine-strict=true
fund=false
# Supply-chain guard: never resolve a version published less than 7 days ago,
# so a hijacked package must survive a week of public scrutiny before it can
# enter this project (npm >= 11.10).
min-release-age=7
